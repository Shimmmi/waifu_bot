# 🔧 Решение проблемы с установкой Cursor Server на удаленном сервере

## Проблема

При подключении Cursor к удаленному серверу через SSH возникает ошибка:
```
[error] Error installing server: [Error: Failed to install server within the timeout
```

Установка Cursor Server не завершается в течение 30 секунд (таймаут).

---

## 🔍 Диагностика

### Шаг 1: Проверка SSH подключения вручную

Выполните в терминале Windows (PowerShell или CMD):

```powershell
ssh rocketcloud-vps
```

Если подключение успешно, вы должны увидеть приглашение командной строки сервера.

**Если не подключается:**
- Проверьте правильность IP адреса в `~/.ssh/config`
- Проверьте, что SSH ключ правильно настроен
- Убедитесь, что сервер доступен: `ping 45.156.21.149`

### Шаг 2: Проверка системных ресурсов на сервере

Подключитесь к серверу и выполните:

```bash
# Проверка свободного места на диске
df -h

# Проверка оперативной памяти
free -h

# Проверка нагрузки системы
top
# (нажмите 'q' для выхода)

# Проверка версии системы
cat /etc/os-release
```

**Требования:**
- Минимум 1 GB свободного места на диске
- Минимум 512 MB свободной оперативной памяти
- Стабильное интернет-соединение

### Шаг 3: Проверка прав доступа

```bash
# Проверка текущего пользователя
whoami

# Проверка прав на домашнюю директорию
ls -la ~

# Проверка прав на /tmp
ls -ld /tmp
```

---

## ✅ Решения

### Решение 1: Увеличение таймаута в Cursor

1. Откройте настройки Cursor: `Ctrl+,`
2. В поиске введите: `remote.SSH.connectTimeout`
3. Установите значение `60` или `120` (в секундах)
4. Сохраните и попробуйте подключиться снова

**Или через файл настроек:**

1. Нажмите `Ctrl+Shift+P`
2. Введите "Preferences: Open User Settings (JSON)"
3. Добавьте:

```json
{
    "remote.SSH.connectTimeout": 120,
    "remote.SSH.serverInstallTimeout": 300
}
```

### Решение 2: Использование не-root пользователя (рекомендуется)

Создайте отдельного пользователя для разработки:

```bash
# На сервере выполните:
adduser developer
usermod -aG sudo developer

# Создайте директорию для SSH ключа
mkdir -p /home/developer/.ssh
chmod 700 /home/developer/.ssh

# Скопируйте ваш публичный ключ (с локальной машины выполните):
# ssh-copy-id developer@45.156.21.149

# Или вручную на сервере:
# nano /home/developer/.ssh/authorized_keys
# (вставьте содержимое вашего ~/.ssh/id_ed25519.pub)
chmod 600 /home/developer/.ssh/authorized_keys
chown -R developer:developer /home/developer/.ssh
```

Обновите `~/.ssh/config`:

```
Host rocketcloud-vps
    HostName 45.156.21.149
    User developer
    IdentityFile ~/.ssh/id_ed25519
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

### Решение 3: Очистка временных файлов на сервере

Подключитесь к серверу и выполните:

```bash
# Очистка временных файлов
rm -rf /tmp/cursor-*
rm -rf /tmp/*.sh
rm -rf ~/.cursor-server-*

# Очистка кэша пакетов (если используется)
apt clean  # для Debian/Ubuntu
# или
yum clean all  # для CentOS/RHEL
```

### Решение 4: Проверка и исправление проблем с сетью

Если соединение медленное или нестабильное:

1. **Проверьте ping:**
   ```powershell
   ping 45.156.21.149
   ```

2. **Проверьте задержку:**
   ```powershell
   Test-NetConnection -ComputerName 45.156.21.149 -Port 22
   ```

3. **Добавьте параметры в SSH config:**
   ```
   Host rocketcloud-vps
       HostName 45.156.21.149
       User root
       IdentityFile ~/.ssh/id_ed25519
       ServerAliveInterval 30
       ServerAliveCountMax 10
       TCPKeepAlive yes
       Compression yes
       ControlMaster auto
       ControlPath ~/.ssh/sockets/%r@%h-%p
       ControlPersist 600
   ```

   Создайте директорию для сокетов:
   ```powershell
   mkdir -p ~/.ssh/sockets
   ```

### Решение 5: Ручная установка Cursor Server

Если автоматическая установка не работает, можно попробовать установить вручную:

1. Подключитесь к серверу:
   ```powershell
   ssh rocketcloud-vps
   ```

2. На сервере выполните:
   ```bash
   # Создайте директорию для Cursor Server
   mkdir -p ~/.cursor-server
   
   # Установите необходимые зависимости
   # Для Debian/Ubuntu:
   apt update && apt install -y curl wget tar gzip
   
   # Для CentOS/RHEL:
   yum install -y curl wget tar gzip
   ```

3. Попробуйте подключиться снова через Cursor

### Решение 6: Проверка файрвола

Убедитесь, что порт 22 (SSH) открыт:

```bash
# На сервере проверьте:
ufw status  # для Ubuntu/Debian с ufw
# или
iptables -L -n  # для других систем

# Если нужно открыть порт:
ufw allow 22/tcp
```

### Решение 7: Альтернативный способ - через SSH туннель

Если прямое подключение не работает, можно использовать SSH туннель:

1. Обновите `~/.ssh/config`:
   ```
   Host rocketcloud-vps
       HostName 45.156.21.149
       User root
       IdentityFile ~/.ssh/id_ed25519
       LocalForward 62217 localhost:62217
       ServerAliveInterval 60
       ServerAliveCountMax 3
   ```

---

## 🚀 Быстрая проверка готовности сервера

Создайте скрипт для проверки на сервере:

```bash
#!/bin/bash
echo "=== System Check ==="
echo "Disk space:"
df -h | grep -E '^/dev|Filesystem'
echo ""
echo "Memory:"
free -h
echo ""
echo "User:"
whoami
echo ""
echo "Python version:"
python3 --version 2>/dev/null || echo "Python not found"
echo ""
echo "Disk space in /tmp:"
df -h /tmp
echo ""
echo "Writable directories:"
[ -w ~ ] && echo "✓ Home directory is writable" || echo "✗ Home directory is NOT writable"
[ -w /tmp ] && echo "✓ /tmp is writable" || echo "✗ /tmp is NOT writable"
```

Сохраните как `check_server.sh` и выполните:
```bash
chmod +x check_server.sh
./check_server.sh
```

---

## 📝 Проверка после исправления

После применения решений:

1. **Закройте все окна Cursor**
2. **Перезапустите Cursor**
3. **Попробуйте подключиться снова:**
   - `Ctrl+Shift+P` → "Remote-SSH: Connect to Host"
   - Выберите `rocketcloud-vps`

4. **Проверьте логи:**
   - `Ctrl+Shift+P` → "Remote-SSH: Show Log"
   - Ищите ошибки или предупреждения

---

## 🔗 Дополнительные ресурсы

- [Официальная документация Remote-SSH](https://code.visualstudio.com/docs/remote/ssh)
- [Troubleshooting Remote-SSH](https://code.visualstudio.com/docs/remote/ssh-troubleshooting)

---

## 💡 Если ничего не помогло

1. **Проверьте версию Cursor:**
   - Убедитесь, что у вас последняя версия
   - Обновите, если нужно

2. **Попробуйте VS Code:**
   - Cursor основан на VS Code
   - Попробуйте подключиться через VS Code для проверки

3. **Обратитесь в поддержку:**
   - Соберите логи: `Ctrl+Shift+P` → "Remote-SSH: Show Log"
   - Приложите к сообщению в поддержку

---

## 📋 Чеклист для диагностики

- [ ] SSH подключение работает вручную
- [ ] Достаточно места на диске (минимум 1 GB)
- [ ] Достаточно оперативной памяти (минимум 512 MB)
- [ ] Порт 22 открыт в файрволе
- [ ] Таймаут увеличен в настройках Cursor
- [ ] Используется корректный пользователь (не root рекомендуется)
- [ ] Временные файлы очищены
- [ ] Сетевое соединение стабильное

