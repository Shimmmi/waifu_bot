# 🔧 Установка Git на сервере RocketCloud

## ❌ Проблема
```
bash: git: command not found
```

## ✅ Решение: Установить Git

### Шаг 1: Обновить список пакетов

В терминале Cursor на сервере выполните:

```bash
apt update
```

### Шаг 2: Установить Git

```bash
apt install -y git
```

### Шаг 3: Проверить установку

```bash
git --version
```

Должно показать что-то вроде: `git version 2.34.1` или выше.

### Шаг 4: Теперь можно клонировать проект

```bash
cd /opt/waifu-bot
git clone https://github.com/Shimmmi/waifu_bot.git .
```

---

## 📋 Полная последовательность команд

Выполните все команды по порядку:

```bash
# 1. Обновить список пакетов
apt update

# 2. Установить Git
apt install -y git

# 3. Проверить установку
git --version

# 4. Перейти в папку проекта
cd /opt/waifu-bot

# 5. Клонировать проект
git clone https://github.com/Shimmmi/waifu_bot.git .

# 6. Проверить, что файлы загружены
ls -la
```

---

## ⚠️ Если папка /opt/waifu-bot уже существует и не пуста

Если при клонировании появится ошибка о том, что папка не пуста:

```bash
# Удалить содержимое папки (если нужно)
cd /opt
rm -rf waifu-bot
mkdir -p waifu-bot
cd waifu-bot

# Теперь клонировать
git clone https://github.com/Shimmmi/waifu_bot.git .
```

---

## ✅ После успешного клонирования

1. **Откройте папку в Cursor:**
   - Нажмите `Ctrl+K Ctrl+O`
   - Введите: `/opt/waifu-bot`
   - Нажмите "OK"

2. **Проверьте структуру проекта:**
   ```bash
   ls -la
   ```

3. **Должны быть видны:**
   - `src/`
   - `webapp/`
   - `requirements.txt`
   - `README.md`
   - и другие файлы

---

**Выполните команды из "Полная последовательность команд" выше!**

