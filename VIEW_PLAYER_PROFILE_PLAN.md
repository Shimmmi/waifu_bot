# 👤 План реализации: Просмотр профиля другого игрока

## Обзор
Документ описывает реализацию функции просмотра профиля другого игрока из различных мест приложения (модальные окна, списки участников клана, результаты событий и т.д.).

---

## 🎯 Цели и задачи

### Основная цель
Позволить игрокам просматривать профили других игроков для:
- Сравнения прогресса и достижений
- Просмотра активной вайфу других игроков
- Изучения коллекции вайфу других игроков
- Социального взаимодействия в игре

### Задачи
1. Создать API endpoint для получения публичных данных профиля игрока
2. Реализовать модальное окно просмотра профиля другого игрока
3. Интегрировать возможность открытия профиля из различных мест:
   - Список участников клана
   - Результаты групповых событий
   - Рейтинги и таблицы лидеров (будущее)
   - Чаты и сообщения (будущее)

---

## 📊 Структура данных профиля

### Публичные данные (доступны для просмотра)
- **Базовая информация:**
  - Имя пользователя (`username`)
  - Отображаемое имя (`display_name`)
  - Уровень аккаунта (`account_level`)
  - Глобальный опыт (`global_xp`)
  - Аватар (загруженный или дефолтный)

- **Валюта (общая информация, без точных цифр):**
  - Золото (`coins`) - отображается округленным (например, "1.2K", "5.5M")
  - Гемы (`gems`) - отображается округленным
  - Очки навыков (`skill_points`) - точное значение

- **Активная вайфу:**
  - ID вайфу
  - Имя вайфу
  - Уровень вайфу
  - Сила (power)
  - Изображение вайфу
  - Редкость вайфу
  - Раса, профессия, национальность

- **Статистика:**
  - Общее количество вайфу в коллекции (без списка)
  - Дата регистрации (опционально)
  - Клан (название и тег, если состоит)

### Приватные данные (НЕ отображаются)
- Точные значения валют (для приватности)
- Полный список вайфу в коллекции
- Детали навыков и их уровни
- Настройки профиля
- История действий

---

## 🔧 Backend API Endpoints

### 1. GET `/api/profile/view/{user_id}`
Получение публичных данных профиля другого игрока.

**Параметры запроса:**
- `user_id` (path): ID пользователя, профиль которого нужно просмотреть
- `initData` (query): initData текущего пользователя (для авторизации)

**Ответ (200 OK):**
```json
{
  "success": true,
  "profile": {
    "user_id": 123,
    "username": "player123",
    "display_name": "Cool Player",
    "account_level": 15,
    "global_xp": 5432,
    "avatar_image": "data:image/png;base64,..." or null,
    "coins": 12500,
    "coins_display": "12.5K",
    "gems": 450,
    "gems_display": "450",
    "skill_points": 42,
    "active_waifu": {
      "id": "wf_abc123",
      "name": "Sakura",
      "level": 25,
      "power": 345,
      "rarity": "Epic",
      "race": "Elf",
      "profession": "Mage",
      "nationality": "Japan",
      "image_url": "https://..."
    },
    "waifu_count": 45,
    "clan": {
      "name": "Legendary Guild",
      "tag": "LG",
      "role": "member" or null
    },
    "joined_at": "2024-01-15T10:30:00Z"
  }
}
```

**Ошибки:**
- `404 Not Found`: Пользователь не найден
- `401 Unauthorized`: Не авторизован
- `403 Forbidden`: Профиль недоступен (если в будущем добавим приватность)

**Файл реализации:** `src/bot/api_server.py`

**Логика:**
1. Проверка авторизации текущего пользователя
2. Поиск целевого пользователя по `user_id`
3. Если пользователь не найден, возврат 404
4. Формирование публичных данных профиля
5. Получение активной вайфу (если есть)
6. Форматирование валют для отображения (округление)
7. Получение информации о клане (если состоит)

---

## 🎨 Frontend UI/UX

### Модальное окно просмотра профиля

#### Структура окна:
```
┌─────────────────────────────────────┐
│  [X]                                │
│                                     │
│  [Аватар]                           │
│  @username                          │
│  Display Name                       │
│                                     │
│  ⭐ Уровень: 15  💰12.5K  💎450    │
│                                     │
│  [Изображение активной вайфу]       │
│  Имя вайфу                          │
│  Уровень 25 • 💪345                │
│  Epic • Elf • Mage • 🇯🇵           │
│                                     │
│  📊 Коллекция: 45 вайфу            │
│  🏰 Клан: Legendary Guild [LG]     │
│                                     │
│  [Назад]                            │
└─────────────────────────────────────┘
```

#### Стиль и дизайн:
- **Фон:** Белый с закругленными углами (border-radius: 20px)
- **Аватар:** Круглый, 80x80px, в центре сверху
- **Имя пользователя:** Жирным, темный цвет (#333)
- **Уровень и валюта:** Компактно, иконки + значения
- **Активная вайфу:** Карточка с изображением, похожая на главную страницу
- **Редкость вайфу:** Цветная рамка по редкости
- **Кнопка "Назад":** Внизу, во всю ширину

#### Адаптивность:
- Максимальная ширина: 400px
- Максимальная высота: 90vh
- Прокрутка при необходимости (overflow-y: auto)
- Отступы: 24px по периметру

---

## 🔌 Интеграция в существующие места

### 1. Список участников клана

**Файл:** `webapp/app.js`

**Изменения в функции `renderClanMembers()`:**
- Добавить обработчик клика на карточку участника
- При клике вызывать `openPlayerProfileModal(user_id)`
- Сделать карточку кликабельной (cursor: pointer, hover эффект)

**Код изменения:**
```javascript
function renderClanMembers(members) {
    const roleEmojis = {
        'leader': '👑',
        'officer': '⭐',
        'member': '👤'
    };
    
    return members.map(m => `
        <div onclick="openPlayerProfileModal(${m.user_id})" style="
            display: flex; align-items: center; padding: 8px; 
            border-bottom: 1px solid #f0f0f0; cursor: pointer;
            transition: background 0.2s;
        " onmouseover="this.style.background='#f8f9fa'" 
           onmouseout="this.style.background='white'">
            <div style="font-size: 24px; margin-right: 8px;">${roleEmojis[m.role]}</div>
            <div style="flex: 1;">
                <div style="font-weight: bold; font-size: 14px; color: #333;">@${m.username}</div>
                <div style="font-size: 12px; color: #666;">В клане с ${new Date(m.joined_at).toLocaleDateString('ru-RU')}</div>
            </div>
            <div style="color: #999; font-size: 18px;">→</div>
        </div>
    `).join('');
}
```

### 2. Результаты групповых событий

**Файл:** `src/bot/services/group_event_system.py` (backend)

**Изменения:**
- В сообщении результатов добавить inline-кнопки для просмотра профилей
- Использовать WebApp URL с параметром `user_id`

**Файл:** `webapp/app.js` (frontend)

**Изменения:**
- При открытии событий из WebApp добавить возможность просмотра профилей участников
- Кнопки/ссылки на профили участников

### 3. Будущие места интеграции:
- Рейтинги и таблицы лидеров
- Комментарии в чатах
- Списки друзей/контактов
- История взаимодействий

---

## 📝 Frontend функции

### `openPlayerProfileModal(user_id)`

**Назначение:** Открыть модальное окно с профилем другого игрока

**Параметры:**
- `user_id` (number): ID пользователя для просмотра

**Логика:**
1. Вызвать API `/api/profile/view/{user_id}`
2. При успехе отобразить модальное окно с данными профиля
3. При ошибке показать alert с сообщением

**Пример кода:**
```javascript
async function openPlayerProfileModal(userId) {
    try {
        const initData = window.Telegram?.WebApp?.initData || '';
        const response = await fetch(`/api/profile/view/${userId}?${new URLSearchParams({ initData })}`);
        
        if (!response.ok) {
            const errorData = await response.json();
            window.Telegram?.WebApp?.showAlert?.('Ошибка: ' + (errorData.detail || 'Пользователь не найден'));
            return;
        }
        
        const data = await response.json();
        const profile = data.profile;
        
        // Создать модальное окно
        const modal = document.createElement('div');
        modal.style.cssText = `
            position: fixed; top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(0,0,0,0.8); display: flex; align-items: center;
            justify-content: center; z-index: 10000; padding: 20px;
        `;
        
        // Получить цвет редкости активной вайфу
        const rarityColors = getRarityColor(profile.active_waifu?.rarity || 'Common');
        
        modal.innerHTML = `
            <div style="background: white; border-radius: 20px; max-width: 400px; width: 100%; max-height: 90vh; overflow-y: auto; padding: 24px; position: relative;">
                <button onclick="closePlayerProfileModal()" style="
                    position: absolute; top: 16px; right: 16px; width: 32px; height: 32px;
                    background: #dc3545; color: white; border: none; border-radius: 50%;
                    font-size: 18px; font-weight: bold; cursor: pointer; display: flex;
                    align-items: center; justify-content: center;
                ">×</button>
                
                <!-- Avatar -->
                <div style="display: flex; flex-direction: column; align-items: center; margin-bottom: 20px;">
                    <div id="player-profile-avatar" style="
                        width: 80px; height: 80px; border-radius: 50%;
                        background-size: cover; background-position: center;
                        margin-bottom: 12px; border: 3px solid #e0e0e0;
                    "></div>
                    <div style="font-weight: bold; font-size: 18px; color: #333; margin-bottom: 4px;">
                        @${profile.username}
                    </div>
                    ${profile.display_name ? `<div style="font-size: 14px; color: #666; margin-bottom: 8px;">${profile.display_name}</div>` : ''}
                </div>
                
                <!-- Level and Currency -->
                <div style="display: flex; justify-content: space-around; margin-bottom: 24px; padding: 12px; background: #f8f9fa; border-radius: 12px;">
                    <div style="text-align: center;">
                        <div style="font-size: 12px; color: #666; margin-bottom: 4px;">⭐ Уровень</div>
                        <div style="font-weight: bold; font-size: 16px; color: #333;">${profile.account_level}</div>
                    </div>
                    <div style="text-align: center;">
                        <div style="font-size: 12px; color: #666; margin-bottom: 4px;">💰 Золото</div>
                        <div style="font-weight: bold; font-size: 16px; color: #333;">${profile.coins_display}</div>
                    </div>
                    <div style="text-align: center;">
                        <div style="font-size: 12px; color: #666; margin-bottom: 4px;">💎 Гемы</div>
                        <div style="font-weight: bold; font-size: 16px; color: #333;">${profile.gems_display}</div>
                    </div>
                </div>
                
                <!-- Active Waifu -->
                ${profile.active_waifu ? `
                <div style="
                    background: ${rarityColors.background};
                    border: 3px solid ${rarityColors.border};
                    border-radius: 16px; padding: 16px; margin-bottom: 20px;
                    box-shadow: 0 2px 8px ${rarityColors.glow};
                ">
                    <div style="text-align: center; margin-bottom: 12px;">
                        <img src="${profile.active_waifu.image_url}" alt="${profile.active_waifu.name}" 
                             style="width: 120px; height: 120px; object-fit: cover; border-radius: 12px; border: 2px solid ${rarityColors.border};"
                             onerror="this.src='data:image/svg+xml,%3Csvg%20xmlns=%27http://www.w3.org/2000/svg%27%20width=%27120%27%20height=%27120%27%3E%3Ctext%20x=%2750%25%27%20y=%2750%25%27%20font-size=%2712%27%20text-anchor=%27middle%27%20dy=%27.3em%27%3E🎭%3C/text%3E%3C/svg%3E'">
                    </div>
                    <div style="font-weight: bold; font-size: 18px; text-align: center; margin-bottom: 8px; color: #333;">
                        ${profile.active_waifu.name}
                    </div>
                    <div style="text-align: center; font-size: 14px; color: #666; margin-bottom: 8px;">
                        Уровень ${profile.active_waifu.level} • 💪${profile.active_waifu.power}
                    </div>
                    <div style="text-align: center; font-size: 12px; color: #999;">
                        ${profile.active_waifu.rarity} • ${profile.active_waifu.race} • ${profile.active_waifu.profession} • ${getFlagEmoji(profile.active_waifu.nationality)}
                    </div>
                </div>
                ` : `
                <div style="text-align: center; padding: 24px; color: #999; font-size: 14px;">
                    Активная вайфу не выбрана
                </div>
                `}
                
                <!-- Statistics -->
                <div style="background: #f8f9fa; border-radius: 12px; padding: 16px; margin-bottom: 20px;">
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
                        <span style="font-size: 14px; color: #666;">📊 Коллекция:</span>
                        <span style="font-weight: bold; font-size: 14px; color: #333;">${profile.waifu_count} вайфу</span>
                    </div>
                    ${profile.clan ? `
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <span style="font-size: 14px; color: #666;">🏰 Клан:</span>
                        <span style="font-weight: bold; font-size: 14px; color: #333;">${profile.clan.name} [${profile.clan.tag}]</span>
                    </div>
                    ` : ''}
                </div>
                
                <!-- Back Button -->
                <button onclick="closePlayerProfileModal()" style="
                    width: 100%; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white; border: none; padding: 14px; border-radius: 12px;
                    font-size: 16px; font-weight: bold; cursor: pointer;
                ">Назад</button>
            </div>
        `;
        
        // Установить аватар
        const avatarElement = modal.querySelector('#player-profile-avatar');
        if (profile.avatar_image) {
            avatarElement.style.backgroundImage = `url(${profile.avatar_image})`;
        } else {
            const avatarNum = ((profile.user_id || 0) % 9) + 1;
            const avatarUrl = `https://raw.githubusercontent.com/Shimmmi/waifu_bot/main/waifu-images/avatars/avatar_${avatarNum}.png`;
            avatarElement.style.backgroundImage = `url(${avatarUrl})`;
        }
        
        modal.addEventListener('click', (e) => {
            if (e.target === modal) {
                closePlayerProfileModal();
            }
        });
        
        document.body.appendChild(modal);
        
    } catch (error) {
        console.error('Error loading player profile:', error);
        window.Telegram?.WebApp?.showAlert?.('Ошибка загрузки профиля');
    }
}

function closePlayerProfileModal() {
    const modals = document.querySelectorAll('div[style*="position: fixed"]');
    modals.forEach(modal => {
        if (modal.style.zIndex === '10000') {
            const innerDiv = modal.querySelector('div[style*="border-radius: 20px"]');
            if (innerDiv && innerDiv.innerHTML.includes('player-profile-avatar')) {
                modal.remove();
            }
        }
    });
}
```

---

## 🔒 Безопасность и приватность

### Текущая реализация:
- Все профили публичны (любой может просмотреть)
- Отображаются только публичные данные
- Точные значения валют округляются для приватности

### Будущие улучшения:
- Настройки приватности профиля (публичный/приватный/только для друзей)
- Блокировка пользователей
- Скрытие определенных данных (валюта, коллекция)

---

## 📋 Чек-лист реализации

### Backend:
- [ ] Создать endpoint `/api/profile/view/{user_id}` в `src/bot/api_server.py`
- [ ] Реализовать логику получения публичных данных профиля
- [ ] Добавить форматирование валют (округление)
- [ ] Добавить получение активной вайфу
- [ ] Добавить получение информации о клане
- [ ] Добавить обработку ошибок (404, 401)
- [ ] Добавить логирование

### Frontend:
- [ ] Создать функцию `openPlayerProfileModal(user_id)` в `webapp/app.js`
- [ ] Создать функцию `closePlayerProfileModal()` в `webapp/app.js`
- [ ] Реализовать UI модального окна с профилем
- [ ] Интегрировать в `renderClanMembers()` (кликабельные карточки)
- [ ] Добавить обработку ошибок и loading states
- [ ] Протестировать на различных устройствах

### Тестирование:
- [ ] Протестировать просмотр профиля из списка участников клана
- [ ] Протестировать просмотр профиля несуществующего пользователя
- [ ] Протестировать просмотр профиля без активной вайфу
- [ ] Протестировать просмотр профиля без клана
- [ ] Протестировать на мобильных устройствах
- [ ] Протестировать производительность при большом количестве участников

---

## 🚀 Будущие улучшения

1. **Расширенный просмотр коллекции:**
   - Просмотр всех вайфу другого игрока (с ограничениями)
   - Фильтры и сортировка

2. **Социальные функции:**
   - Добавление в друзья
   - Отправка сообщений
   - Подарки

3. **Сравнение профилей:**
   - Сравнение прогресса и статистики
   - Сравнение коллекций

4. **Рейтинги:**
   - Глобальный рейтинг игроков
   - Рейтинг по различным параметрам (сила вайфу, уровень, коллекция)

5. **Настройки приватности:**
   - Публичный/приватный профиль
   - Скрытие определенных данных
   - Блокировка пользователей

---

## 📚 Связанные файлы

### Backend:
- `src/bot/api_server.py` - API endpoint для просмотра профиля
- `src/bot/models.py` - Модели User, Waifu, ClanMember

### Frontend:
- `webapp/app.js` - Функции просмотра профиля и интеграция
- `webapp/index.html` - HTML структура (если нужно)

### Документация:
- `SKILLS_DETAILED_ANALYSIS.md` - Пример формата документации
- `DESIGN_PLANS.md` - Общие планы развития

---

## 📝 Заметки по реализации

1. **Форматирование валют:**
   - Меньше 1000: точное значение (например, "450")
   - От 1000 до 999999: округление до тысяч (например, "12.5K")
   - От 1M: округление до миллионов (например, "5.2M")

2. **Обработка отсутствия данных:**
   - Если активной вайфу нет, показать сообщение "Активная вайфу не выбрана"
   - Если клана нет, не показывать раздел клана
   - Если аватара нет, использовать дефолтный аватар

3. **Производительность:**
   - Кэширование данных профиля на короткое время (если необходимо)
   - Ленивая загрузка изображений
   - Оптимизация запросов к базе данных

---

**Дата создания:** 2024-01-XX
**Автор:** AI Assistant
**Статус:** Готов к реализации
