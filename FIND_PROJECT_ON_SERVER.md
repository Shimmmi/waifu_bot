# 📁 Как найти папку проекта на сервере

## ✅ Стандартная папка проекта

Обычно проект находится в: **`/opt/waifu-bot`**

## 🔍 Как проверить и найти проект

### Вариант 1: Через терминал в Cursor (рекомендуется)

1. **Откройте терминал в Cursor:**
   - Нажмите `Ctrl+`` ` (обратная кавычка)
   - Или Terminal → New Terminal

2. **Проверьте, существует ли папка `/opt/waifu-bot`:**
   ```bash
   ls -la /opt/waifu-bot
   ```

3. **Если папка существует, откройте её:**
   ```bash
   cd /opt/waifu-bot
   ls -la
   ```

4. **Если видите файлы проекта (src/, requirements.txt, .env и т.д.), откройте папку в Cursor:**
   - Нажмите `Ctrl+K Ctrl+O` (или File → Open Folder)
   - Введите: `/opt/waifu-bot`
   - Нажмите "OK"

### Вариант 2: Поиск проекта по всему серверу

Если папка `/opt/waifu-bot` не существует, поищите проект:

```bash
# Поиск папки waifu-bot
find / -type d -name "waifu-bot" 2>/dev/null

# Поиск папки с файлом requirements.txt
find / -name "requirements.txt" -type f 2>/dev/null | grep -v "/proc\|/sys"

# Поиск папки с файлом .env
find / -name ".env" -type f 2>/dev/null | grep -v "/proc\|/sys"
```

### Вариант 3: Проверка домашних директорий

```bash
# Проверка в /root
ls -la /root

# Проверка в /home
ls -la /home

# Поиск всех папок с Python проектами
find /home /root -name "*.py" -type f 2>/dev/null | head -5
```

## 📋 Если проект не найден

Если проект еще не развернут на сервере, нужно:

1. **Создать папку проекта:**
   ```bash
   mkdir -p /opt/waifu-bot
   cd /opt/waifu-bot
   ```

2. **Клонировать репозиторий (если используете Git):**
   ```bash
   git clone YOUR_REPOSITORY_URL .
   ```

3. **Или загрузить файлы через SCP с локального компьютера:**
   ```powershell
   # На локальном компьютере (Windows)
   scp -r * root@45.156.21.149:/opt/waifu-bot/
   ```

## 🎯 Что делать прямо сейчас

1. **Откройте терминал в Cursor** (`Ctrl+`` `)
2. **Выполните команду:**
   ```bash
   ls -la /opt/waifu-bot
   ```
3. **Если папка существует и содержит файлы проекта:**
   - Нажмите `Ctrl+K Ctrl+O`
   - Введите: `/opt/waifu-bot`
   - Нажмите "OK"
4. **Если папка не существует или пуста:**
   - Скажите мне, и я помогу развернуть проект

## 📝 Типичная структура проекта

Проект должен содержать:
```
/opt/waifu-bot/
├── src/
│   └── bot/
│       ├── __init__.py
│       ├── main.py
│       ├── config.py
│       └── ...
├── webapp/
├── requirements.txt
├── .env
├── README.md
└── ...
```

Если видите такую структуру - это правильная папка!

