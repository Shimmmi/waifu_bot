# 🖥️ Руководство по использованию аварийной консоли RocketCloud

## 📋 Содержание

1. [Доступ к аварийной консоли](#1-доступ-к-аварийной-консоли)
2. [Вход в систему](#2-вход-в-систему)
3. [Основные команды](#3-основные-команды)
4. [Восстановление SSH доступа](#4-восстановление-ssh-доступа)
5. [Диагностика проблем](#5-диагностика-проблем)
6. [Часто задаваемые вопросы](#6-часто-задаваемые-вопросы)

---

## 1. Доступ к аварийной консоли

### 1.1 Через панель управления RocketCloud

1. **Войдите в панель управления RocketCloud:**
   - Откройте https://rocketcloud.ru/
   - Войдите в свой аккаунт

2. **Откройте список ваших серверов:**
   - Перейдите в раздел "VPS" или "Серверы"
   - Найдите ваш сервер

3. **Откройте консоль:**
   - Нажмите на ваш сервер
   - Найдите кнопку "Console" или "Консоль" или "VNC Console"
   - Нажмите на неё

4. **Откроется окно с консолью:**
   - Вы увидите черный экран или приглашение для входа
   - Если видите `server-wtbcmr login:`, это нормально - переходите к следующему разделу

---

## 2. Вход в систему

### 2.1 Запрос логина

Когда вы видите:
```
server-wtbcmr login:
```

Это означает, что система запрашивает имя пользователя для входа.

### 2.2 Варианты входа

#### Вариант A: Вход как root (самый распространенный)

1. **Введите имя пользователя:**
   ```
   root
   ```
   Нажмите `Enter`

2. **Введите пароль:**
   - После ввода имени пользователя появится запрос пароля
   - Введите пароль (пароль не отображается при вводе - это нормально!)
   - Нажмите `Enter`

**Откуда взять пароль root:**
- Если вы устанавливали пароль при создании сервера - используйте его
- Если вы получали пароль по email при создании сервера - проверьте почту
- Если вы меняли пароль - используйте новый пароль

#### Вариант B: Вход под другим пользователем

Если у вас есть другой пользователь с правами sudo:

1. **Введите имя пользователя:**
   ```
   ваш_пользователь
   ```
   Нажмите `Enter`

2. **Введите пароль:**
   - Введите пароль пользователя
   - Нажмите `Enter`

3. **После входа получите права root:**
   ```bash
   sudo su -
   # Или
   sudo -i
   ```

#### Вариант C: Восстановление пароля (если забыли)

Если вы забыли пароль root, можно его сбросить:

1. **Через панель RocketCloud:**
   - Найдите ваш сервер в панели
   - Откройте настройки сервера
   - Найдите опцию "Reset Password" или "Сбросить пароль"
   - Следуйте инструкциям

2. **Через загрузочное меню (если доступно):**
   - Перезагрузите сервер через панель
   - В консоли во время загрузки нажмите нужную клавишу для входа в GRUB
   - Загрузитесь в режиме восстановления
   - Сбросьте пароль

---

## 3. Основные команды

### 3.1 После входа в систему

После успешного входа вы увидите приглашение командной строки:
```bash
root@server-wtbcmr:~#
```

Это означает, что вы успешно вошли в систему как root.

### 3.2 Проверка статуса системы

```bash
# Проверка загрузки системы
uptime

# Проверка использования ресурсов
free -h
df -h

# Проверка активных процессов
ps aux | head -20

# Проверка системных сервисов
systemctl status
```

### 3.3 Проверка SSH сервиса

```bash
# Проверка статуса SSH
systemctl status ssh
# или
systemctl status sshd

# Проверка, запущен ли SSH
systemctl is-active ssh

# Просмотр логов SSH
journalctl -u ssh -n 50
# или
tail -50 /var/log/auth.log
```

### 3.4 Проверка сетевых подключений

```bash
# Проверка сетевых интерфейсов
ip addr show
# или
ifconfig

# Проверка открытых портов
netstat -tlnp | grep :22
# или
ss -tlnp | grep :22

# Проверка файрвола
ufw status
# или
iptables -L -n
```

---

## 4. Восстановление SSH доступа

### 4.1 Если SSH сервис не запущен

```bash
# Запуск SSH сервиса
systemctl start ssh

# Включение автозапуска SSH
systemctl enable ssh

# Проверка статуса
systemctl status ssh
```

### 4.2 Если SSH заблокирован файрволом

```bash
# Если используется ufw
ufw allow 22/tcp
ufw reload

# Если используется iptables
iptables -A INPUT -p tcp --dport 22 -j ACCEPT
iptables-save

# Проверка
ufw status
# или
iptables -L -n | grep 22
```

### 4.3 Если проблема с SSH ключами

```bash
# Проверка наличия SSH ключей
ls -la ~/.ssh/

# Проверка прав доступа
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Проверка содержимого authorized_keys
cat ~/.ssh/authorized_keys
```

### 4.4 Если нужно добавить новый SSH ключ

```bash
# Создайте директорию (если не существует)
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ
echo "ВАШ_ПУБЛИЧНЫЙ_SSH_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# Проверка
cat ~/.ssh/authorized_keys
```

### 4.5 Перезапуск SSH сервиса

```bash
# Перезапуск SSH
systemctl restart ssh

# Проверка статуса
systemctl status ssh

# Просмотр логов
journalctl -u ssh -f
```

---

## 5. Диагностика проблем

### 5.1 Проверка загрузки системы

```bash
# Просмотр последних сообщений загрузки
dmesg | tail -50

# Просмотр системных логов
journalctl -n 50

# Проверка ошибок в логах
journalctl -p err -n 50
```

### 5.2 Проверка использования ресурсов

```bash
# Память
free -h

# Диск
df -h

# Процессы, потребляющие память
ps aux --sort=-%mem | head -10

# Процессы, потребляющие CPU
ps aux --sort=-%cpu | head -10
```

### 5.3 Проверка сетевых проблем

```bash
# Проверка сетевых интерфейсов
ip addr show

# Проверка маршрутизации
ip route show

# Проверка DNS
cat /etc/resolv.conf

# Тест подключения
ping -c 4 8.8.8.8
```

### 5.4 Проверка системных сервисов

```bash
# Список всех сервисов
systemctl list-units --type=service

# Список неудачных сервисов
systemctl --failed

# Проверка конкретного сервиса
systemctl status имя_сервиса

# Логи сервиса
journalctl -u имя_сервиса -n 50
```

---

## 6. Часто задаваемые вопросы

### Q1: Не могу войти - пароль не подходит

**Решение:**
1. Убедитесь, что используете правильную раскладку клавиатуры (английская/русская)
2. Проверьте, что Caps Lock выключен
3. Попробуйте сбросить пароль через панель RocketCloud
4. Проверьте, что вводите пароль для правильного пользователя (root или другой)

### Q2: После ввода пароля ничего не происходит

**Решение:**
- Это нормально! Пароль не отображается при вводе для безопасности
- Просто введите пароль и нажмите Enter
- Если пароль правильный, вы увидите приглашение командной строки

### Q3: Консоль не отвечает / зависает

**Решение:**
1. Попробуйте обновить страницу с консолью
2. Закройте и снова откройте консоль
3. Попробуйте другой браузер
4. Проверьте, что сервер не перегружен (используйте команды из раздела 5.2)

### Q4: Вижу только черный экран

**Решение:**
1. Нажмите `Enter` несколько раз
2. Попробуйте ввести `root` и нажать Enter
3. Обновите страницу консоли
4. Проверьте, что сервер запущен (через панель RocketCloud)

### Q5: Как выйти из консоли?

**Решение:**
- Просто закройте окно консоли в браузере
- Или нажмите кнопку "Закрыть" в интерфейсе консоли
- Выход из сессии Linux: `exit` или `logout`

### Q6: Как скопировать текст в/из консоли?

**Решение:**
- **В консоль:** Обычно можно вставить текст через контекстное меню (правая кнопка мыши) или `Ctrl+V`
- **Из консоли:** Выделите текст мышью (обычно автоматически копируется) или используйте контекстное меню
- Некоторые консоли поддерживают `Ctrl+Shift+C` для копирования и `Ctrl+Shift+V` для вставки

### Q7: Не могу найти консоль в панели RocketCloud

**Решение:**
1. Убедитесь, что вы вошли в правильный аккаунт
2. Проверьте, что сервер находится в вашем списке
3. Откройте детальную информацию о сервере
4. Ищите вкладки: "Console", "VNC", "Консоль", "Terminal"
5. Если не можете найти, обратитесь в поддержку RocketCloud

### Q8: Сервер не загружается / показывает ошибки

**Решение:**
1. Сделайте скриншот ошибки
2. Проверьте логи загрузки (см. раздел 5.1)
3. Попробуйте перезагрузить сервер через панель
4. Если проблема сохраняется, обратитесь в поддержку с логами

---

## 7. Полезные команды для аварийного восстановления

### 7.1 Восстановление сетевого подключения

```bash
# Перезапуск сети
systemctl restart networking
# или
systemctl restart NetworkManager

# Проверка сетевых интерфейсов
ip link show

# Включение интерфейса (если отключен)
ip link set eth0 up
```

### 7.2 Очистка места на диске

```bash
# Проверка использования диска
df -h

# Очистка кэша пакетов
apt clean

# Очистка старых логов
journalctl --vacuum-time=1d

# Поиск больших файлов
du -h --max-depth=1 / | sort -hr | head -10
```

### 7.3 Перезагрузка системы

```bash
# Перезагрузка
reboot

# Выключение
poweroff

# Перезагрузка через systemctl
systemctl reboot
```

### 7.4 Мониторинг в реальном времени

```bash
# Мониторинг процессов
top
# или
htop

# Мониторинг использования диска
watch -n 2 df -h

# Мониторинг сети
watch -n 2 'netstat -tuln | grep :22'
```

---

## 8. Безопасность

### 8.1 Смена пароля root

```bash
# Смена пароля
passwd root

# Введите новый пароль дважды
```

### 8.2 Создание нового пользователя с sudo

```bash
# Создание пользователя
adduser новый_пользователь

# Добавление в группу sudo
usermod -aG sudo новый_пользователь

# Проверка
groups новый_пользователь
```

### 8.3 Настройка SSH для безопасности

```bash
# Отредактируйте конфигурацию SSH
nano /etc/ssh/sshd_config

# Рекомендуемые настройки:
# PermitRootLogin no (или PermitRootLogin prohibit-password)
# PasswordAuthentication no (только ключи)
# Port 22 (или другой порт)

# Перезапустите SSH
systemctl restart ssh
```

---

## 📝 Быстрая справка

### Вход в систему:
```
server-wtbcmr login: root
Password: [введите пароль, он не отображается]
```

### Проверка SSH:
```bash
systemctl status ssh
systemctl start ssh
systemctl enable ssh
```

### Проверка файрвола:
```bash
ufw status
ufw allow 22/tcp
```

### Просмотр логов:
```bash
journalctl -u ssh -n 50
tail -50 /var/log/auth.log
```

---

## 🆘 Контакты поддержки

Если вы не можете решить проблему самостоятельно:

1. **Поддержка RocketCloud:**
   - Email: support@rocketcloud.ru
   - Телефон: [указан на сайте]
   - Чат в панели управления

2. **При обращении в поддержку укажите:**
   - IP адрес сервера
   - Описание проблемы
   - Скриншоты ошибок (если есть)
   - Вывод команд диагностики

---

**Последнее обновление:** 2024
**Версия инструкции:** 1.0

