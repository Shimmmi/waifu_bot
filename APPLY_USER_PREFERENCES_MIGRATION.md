# 🚀 Применение миграции для настройки сортировки

## Процесс

Эта миграция добавляет возможность сохранять предпочтения сортировки вайфу для каждого пользователя.

### Что делает миграция:
- Добавляет поле `waifu_sort_preference` в таблицу `users`
- Устанавливает значение по умолчанию `'name'` для всех существующих пользователей
- Создает индекс для оптимизации запросов

### Как применить миграцию

**Способ 1: Через Neon Console**
1. Откройте **Render Dashboard** → ваш проект
2. Найдите вашу базу данных Neon PostgreSQL
3. Откройте **Neon Console** (кнопка "Console" или "Query")
4. Скопируйте содержимое файла `sql/011_add_user_preferences.sql`
5. Вставьте в консоль и нажмите "Run" или "Execute"
6. Дождитесь успешного выполнения

**Способ 2: Через внешний SQL-клиент**
1. Получите connection string из Render Dashboard
2. Подключитесь к базе данных через pgAdmin, DBeaver или другой клиент
3. Выполните SQL из `sql/011_add_user_preferences.sql`

### Проверка миграции

После применения миграции проверьте:
```sql
-- Проверить, что поле добавлено
SELECT column_name, data_type, character_maximum_length 
FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'waifu_sort_preference';

-- Проверить значения по умолчанию
SELECT id, waifu_sort_preference FROM users LIMIT 5;
```

### Что дальше?

После применения миграции:
1. Перезапустите бот на Render (если нужно)
2. Откройте WebApp
3. Попробуйте изменить сортировку вайфу
4. Перезагрузите страницу - сортировка должна сохраниться! 🎉

### Функции после миграции:

✅ **Сохранение сортировки**
- Выбранная сортировка сохраняется в базу данных
- Сохраняется при перезапуске бота
- Синхронизируется между устройствами

✅ **Кнопка обновления**
- Круглая кнопка 🔄 в правом верхнем углу
- Обновляет данные текущей страницы
- Вращается во время загрузки
- Работает на всех страницах (Профиль, Вайфу, Навыки)

### Поддержка

Если миграция не применена, система будет работать с сортировкой по умолчанию (`name`).

