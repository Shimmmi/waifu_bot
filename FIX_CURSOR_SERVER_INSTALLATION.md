# 🔧 Исправление проблемы установки Cursor Server

## 🔍 Диагностика

Проверка показала следующие проблемы на сервере:

1. **❌ Критически мало памяти:** Доступно только 30 MB из 961 MB (используется 931 MB)
2. **⚠️ Медленное соединение:** 60.5 секунд для простой команды
3. **❌ Нет swap:** Swap = 0 MB

**Cursor Server требует минимум 512 MB свободной памяти для установки!**

---

## ✅ Решение

### Шаг 1: Обновить SSH конфигурацию и настройки Cursor ✅ (УЖЕ СДЕЛАНО)

✅ **SSH конфигурация:** Обновлен `~/.ssh/config` с увеличенными таймаутами  
✅ **Настройки Cursor:** Автоматически обновлены через скрипт `update_cursor_settings.ps1`

**Применённые настройки:**
- `remote.SSH.connectTimeout`: 120 секунд
- `remote.SSH.serverInstallTimeout`: 300 секунд
- `remote.SSH.remoteServerListenOnSocket`: true
- `remote.SSH.useLocalServer`: false

**Файл настроек:** `C:\Users\KhazarzhanTV.SPROJECT\AppData\Roaming\Cursor\User\settings.json`

⚠️ **ВАЖНО:** Перезапустите Cursor, чтобы настройки вступили в силу!

### Шаг 2: Исправить проблемы на сервере

Подключитесь к серверу:

```powershell
ssh rocketcloud-vps
```

На сервере выполните следующие команды:

#### 2.1. Проверка текущего состояния

```bash
# Проверка памяти
free -h

# Проверка диска
df -h

# Процессы, потребляющие память
ps aux --sort=-%mem | head -10
```

#### 2.2. Очистка памяти и создание swap

```bash
# 1. Очистка кэша пакетов
apt clean

# 2. Очистка старых логов (оставит только последние 24 часа)
journalctl --vacuum-time=1d

# 3. Очистка временных файлов
rm -rf /tmp/cursor-*
rm -rf /tmp/*.sh
find /tmp -type f -mtime +7 -delete

# 4. Создание swap файла (2GB)
fallocate -l 2G /swapfile
# Если fallocate не работает, используйте:
# dd if=/dev/zero of=/swapfile bs=1M count=2048

# 5. Настройка прав и активация swap
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# 6. Добавление в fstab для постоянства
echo "/swapfile none swap sw 0 0" >> /etc/fstab

# 7. Настройка swappiness (оптимизация)
echo 10 > /proc/sys/vm/swappiness
echo "vm.swappiness=10" >> /etc/sysctl.conf

# 8. Освобождение кэша памяти
sync
echo 3 > /proc/sys/vm/drop_caches
```

#### 2.3. Проверка результатов

```bash
# Проверка памяти (должно быть больше свободной памяти)
free -h

# Проверка swap
swapon --show

# Проверка диска
df -h
```

#### 2.4. Остановка ненужных процессов (если нужно)

Если памяти всё ещё мало, найдите и остановите ненужные процессы:

```bash
# Просмотр процессов
ps aux --sort=-%mem | head -20

# Если нужно остановить какой-то процесс:
# systemctl stop имя_сервиса
# или
# kill PID_процесса
```

**Будьте осторожны!** Не останавливайте системные процессы!

---

## 🚀 Альтернативный способ: Использование скрипта

Я создал скрипт `fix_server_resources.sh`, который автоматически выполнит все шаги:

1. **Скопируйте скрипт на сервер вручную:**

   Откройте файл `fix_server_resources.sh` в редакторе и скопируйте его содержимое.

2. **На сервере создайте файл:**

   ```bash
   nano /root/fix_server_resources.sh
   ```

3. **Вставьте содержимое скрипта и сохраните** (`Ctrl+O`, `Enter`, `Ctrl+X`)

4. **Запустите скрипт:**

   ```bash
   chmod +x /root/fix_server_resources.sh
   bash /root/fix_server_resources.sh
   ```

---

## 📝 После исправления

1. **Выйдите из SSH сессии:**
   ```bash
   exit
   ```

2. **Закройте все окна Cursor**

3. **Перезапустите Cursor**

4. **Попробуйте подключиться снова:**
   - `Ctrl+Shift+P` → "Remote-SSH: Connect to Host"
   - Выберите `rocketcloud-vps`

5. **Проверьте логи при подключении:**
   - `Ctrl+Shift+P` → "Remote-SSH: Show Log"
   - Ищите ошибки или успешное подключение

---

## 🔍 Если проблема сохраняется

### Проверка 1: Достаточно ли памяти после исправления?

```bash
ssh rocketcloud-vps "free -h"
```

Должно быть **минимум 200-300 MB свободной памяти** (available).

### Проверка 2: Работает ли swap?

```bash
ssh rocketcloud-vps "swapon --show"
```

Должен показать `/swapfile` с размером 2GB.

### Проверка 3: Проверка соединения

```powershell
# Проверка ping
ping 45.156.21.220

# Проверка SSH
ssh -v rocketcloud-vps "echo 'test'"
```

### Проверка 4: Просмотр логов Cursor

В Cursor:
- `Ctrl+Shift+P` → "Remote-SSH: Show Log"
- Ищите сообщения об ошибках или таймаутах

---

## 💡 Дополнительные рекомендации

### Если памяти всё ещё не хватает:

1. **Увеличьте swap до 4GB:**
   ```bash
   swapoff /swapfile
   rm /swapfile
   fallocate -l 4G /swapfile
   chmod 600 /swapfile
   mkswap /swapfile
   swapon /swapfile
   ```

2. **Рассмотрите возможность обновления тарифа сервера:**
   - Текущий сервер: 1 GB RAM
   - Рекомендуется: минимум 2 GB RAM для комфортной работы

3. **Оптимизируйте использование памяти:**
   - Остановите неиспользуемые сервисы
   - Используйте более легковесные альтернативы

---

## ✅ Ожидаемый результат

После выполнения всех шагов:

- ✅ Свободная память: минимум 200-300 MB
- ✅ Swap файл: 2 GB активен
- ✅ Таймауты увеличены в Cursor
- ✅ SSH соединение стабильное
- ✅ Cursor Server устанавливается успешно

---

## 📞 Если ничего не помогает

1. **Проверьте логи Cursor:**
   - `Ctrl+Shift+P` → "Remote-SSH: Show Log"
   - Сохраните логи в файл

2. **Попробуйте VS Code:**
   - Cursor основан на VS Code
   - Попробуйте подключиться через VS Code для проверки

3. **Обратитесь в поддержку:**
   - Приложите логи Cursor
   - Приложите вывод `free -h` и `df -h` с сервера
   - Опишите проблему подробно

