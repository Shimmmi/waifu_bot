# 📤 Загрузка проекта на сервер RocketCloud

## 🎯 Ваша ситуация
- Проект находится локально: `C:\Users\user\YandexDisk\Home\Python\Waifu_bot`
- Нужно загрузить его на сервер: `45.156.21.149` в папку `/opt/waifu-bot`

## ✅ Вариант 1: Через GitHub (рекомендуется, если проект уже в GitHub)

Если ваш проект уже загружен в GitHub:

### На сервере (в терминале Cursor, подключенного к серверу):

1. **Откройте терминал в Cursor** (на сервере): `Ctrl+`` `

2. **Создайте папку для проекта:**
   ```bash
   mkdir -p /opt/waifu-bot
   cd /opt/waifu-bot
   ```

3. **Клонируйте репозиторий:**
   ```bash
   git clone YOUR_GITHUB_REPOSITORY_URL .
   ```
   
   Например:
   ```bash
   git clone https://github.com/your-username/waifu-bot.git .
   ```
   
   Или если используете SSH:
   ```bash
   git clone git@github.com:your-username/waifu-bot.git .
   ```

4. **Проверьте, что файлы загружены:**
   ```bash
   ls -la
   ```

5. **Откройте папку в Cursor:**
   - Нажмите `Ctrl+K Ctrl+O`
   - Введите: `/opt/waifu-bot`
   - Нажмите "OK"

---

## ✅ Вариант 2: Через SCP (если проект НЕ в GitHub)

Если проект еще не загружен в GitHub, можно скопировать файлы напрямую:

### Шаг 1: На локальном компьютере (PowerShell)

1. **Откройте PowerShell** в папке проекта:
   ```powershell
   cd C:\Users\user\YandexDisk\Home\Python\Waifu_bot
   ```

2. **Создайте папку на сервере и загрузите файлы:**
   ```powershell
   # Создать папку на сервере
   ssh rocketcloud-vps "mkdir -p /opt/waifu-bot"
   
   # Загрузить файлы (исключая .git, venv, __pycache__)
   scp -r -o "StrictHostKeyChecking=no" `
       --exclude='.git' `
       --exclude='venv' `
       --exclude='__pycache__' `
       --exclude='*.pyc' `
       --exclude='.env' `
       * root@45.156.21.149:/opt/waifu-bot/
   ```

   **Или проще - загрузить все файлы:**
   ```powershell
   # Используя rsync (если установлен) или простой способ
   scp -r src/ webapp/ *.py *.txt *.md *.json *.yaml *.yml *.bat *.sh root@45.156.21.149:/opt/waifu-bot/
   ```

### Шаг 2: Альтернативный способ - через tar и scp

1. **На локальном компьютере (PowerShell):**
   ```powershell
   cd C:\Users\user\YandexDisk\Home\Python\Waifu_bot
   
   # Создать архив (исключая ненужные папки)
   # Используя 7-Zip или WinRAR, создайте архив проекта
   # Или используйте Git Bash:
   ```

2. **Если у вас Git Bash:**
   ```bash
   cd /c/Users/user/YandexDisk/Home/Python/Waifu_bot
   tar --exclude='.git' --exclude='venv' --exclude='__pycache__' --exclude='*.pyc' -czf ../waifu-bot.tar.gz .
   scp ../waifu-bot.tar.gz root@45.156.21.149:/tmp/
   ```

3. **На сервере (в терминале Cursor):**
   ```bash
   mkdir -p /opt/waifu-bot
   cd /opt/waifu-bot
   tar -xzf /tmp/waifu-bot.tar.gz
   rm /tmp/waifu-bot.tar.gz
   ```

---

## ✅ Вариант 3: Через VS Code/Cursor Remote (самый простой!)

Если вы уже подключены к серверу через Remote SSH в Cursor:

1. **На сервере в терминале Cursor:**
   ```bash
   mkdir -p /opt/waifu-bot
   cd /opt/waifu-bot
   ```

2. **На локальном компьютере откройте второй терминал PowerShell:**
   ```powershell
   cd C:\Users\user\YandexDisk\Home\Python\Waifu_bot
   ```

3. **Загрузите файлы:**
   ```powershell
   # Загрузить все файлы проекта
   scp -r -o "StrictHostKeyChecking=no" src/ webapp/ *.py *.txt *.md *.json *.yaml *.yml *.bat *.sh root@45.156.21.149:/opt/waifu-bot/
   ```

4. **В Cursor на сервере обновите файлы (View → Refresh) или откройте папку:**
   - `Ctrl+K Ctrl+O`
   - Введите: `/opt/waifu-bot`

---

## 📋 Рекомендуемый порядок действий

### Если проект в GitHub:

1. **В терминале Cursor на сервере:**
   ```bash
   mkdir -p /opt/waifu-bot
   cd /opt/waifu-bot
   git clone YOUR_REPOSITORY_URL .
   ```

2. **Откройте папку в Cursor:**
   - `Ctrl+K Ctrl+O` → `/opt/waifu-bot`

### Если проект НЕ в GitHub:

1. **Сначала загрузите проект в GitHub** (рекомендуется):
   ```powershell
   cd C:\Users\user\YandexDisk\Home\Python\Waifu_bot
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin YOUR_GITHUB_REPOSITORY_URL
   git push -u origin main
   ```

2. **Затем клонируйте на сервер** (как в варианте 1)

---

## 🔍 Проверка после загрузки

После загрузки проекта на сервер, проверьте:

```bash
cd /opt/waifu-bot
ls -la

# Должны быть видны:
# - src/
# - webapp/
# - requirements.txt
# - README.md
# и другие файлы проекта
```

---

## ⚠️ Важно

1. **Не загружайте файл `.env`** на сервер через SCP - создайте его вручную на сервере с правильными настройками
2. **Не загружайте папку `venv`** - создайте виртуальное окружение на сервере заново
3. **Не загружайте `.git`** если используете SCP (если используете git clone, это нормально)

---

## 📝 Что делать после загрузки

После загрузки проекта на сервер:

1. **Создайте виртуальное окружение:**
   ```bash
   cd /opt/waifu-bot
   python3.11 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

2. **Создайте файл `.env`:**
   ```bash
   nano /opt/waifu-bot/.env
   ```
   (добавьте нужные переменные окружения)

3. **Проверьте структуру проекта:**
   ```bash
   ls -la /opt/waifu-bot
   ```

---

**Какой вариант вы хотите использовать?** Если проект уже в GitHub - используйте вариант 1. Если нет - скажите, и я помогу загрузить проект в GitHub или использовать SCP.

