# 🎯 Полная инструкция по настройке Cursor на новом компьютере для проекта Waifu Bot

## 📋 Содержание

1. [Установка Cursor](#1-установка-cursor)
2. [Настройка SSH для подключения к серверу](#2-настройка-ssh-для-подключения-к-серверу)
3. [Настройка Remote SSH в Cursor](#3-настройка-remote-ssh-в-cursor)
4. [Установка необходимых расширений](#4-установка-необходимых-расширений)
5. [Настройка Python окружения](#5-настройка-python-окружения)
6. [Настройка проекта](#6-настройка-проекта)
7. [Настройка Git](#7-настройка-git)
8. [Проверка работоспособности](#8-проверка-работоспособности)
9. [Полезные команды и настройки](#9-полезные-команды-и-настройки)
10. [Устранение проблем](#10-устранение-проблем)

---

## 1. Установка Cursor

### 1.1 Скачивание и установка

1. **Перейдите на официальный сайт Cursor:**
   - Откройте браузер
   - Перейдите по адресу: https://cursor.sh/
   - Нажмите "Download" или "Download for Windows"

2. **Установка:**
   - Запустите скачанный установочный файл
   - Следуйте инструкциям установщика
   - При установке можете выбрать:
     - ✅ "Add to PATH" (добавить в PATH)
     - ✅ "Create desktop shortcut" (создать ярлык на рабочем столе)
     - ✅ "Create Start Menu entry" (создать запись в меню Пуск)

3. **Первый запуск:**
   - Запустите Cursor
   - При первом запуске выберите тему (Light/Dark)
   - Закройте окно приветствия

---

## 2. Настройка SSH для подключения к серверу

### 2.1 Генерация SSH ключа (если его еще нет)

1. **Откройте PowerShell или Git Bash:**
   - Нажмите `Win + X` → выберите "Windows PowerShell" или "Terminal"
   - Или используйте Git Bash (если установлен)

2. **Проверьте, есть ли уже SSH ключи:**
   ```powershell
   ls ~/.ssh/
   ```
   
   Если видите файлы `id_ed25519` и `id_ed25519.pub` или `id_rsa` и `id_rsa.pub`, значит ключи уже есть. Переходите к шагу 2.2.

3. **Создайте новый SSH ключ:**
   ```powershell
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```
   
   - Нажмите `Enter` для использования пути по умолчанию (`C:\Users\ВашеИмя\.ssh\id_ed25519`)
   - Введите парольную фразу (или оставьте пустым, нажав `Enter`)
   - Подтвердите парольную фразу (или нажмите `Enter`)

4. **Запуск SSH агента:**
   ```powershell
   # В PowerShell
   Start-Service ssh-agent
   
   # Добавление ключа в агент
   ssh-add ~/.ssh/id_ed25519
   ```

### 2.2 Копирование публичного ключа на сервер

**Вариант A: Если у вас есть доступ к серверу другим способом (веб-консоль, другой компьютер)**

1. **Просмотрите публичный ключ:**
   ```powershell
   cat ~/.ssh/id_ed25519.pub
   ```
   
2. **Скопируйте весь вывод** (начинается с `ssh-ed25519` и заканчивается вашим email)

3. **На сервере выполните:**
   ```bash
   # Создайте директорию .ssh (если не существует)
   mkdir -p ~/.ssh
   chmod 700 ~/.ssh
   
   # Добавьте публичный ключ
   echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
   chmod 600 ~/.ssh/authorized_keys
   ```

**Вариант B: Если у вас есть старый компьютер с рабочим SSH ключом**

1. **Скопируйте приватный ключ со старого компьютера:**
   - Скопируйте файл `~/.ssh/id_ed25519` (или `id_rsa`)
   - Скопируйте файл `~/.ssh/id_ed25519.pub` (или `id_rsa.pub`)
   - Поместите их в `C:\Users\ВашеИмя\.ssh\` на новом компьютере

2. **Установите правильные права доступа (Windows):**
   ```powershell
   # Откройте PowerShell от имени администратора
   icacls "$env:USERPROFILE\.ssh\id_ed25519" /inheritance:r
   icacls "$env:USERPROFILE\.ssh\id_ed25519" /grant:r "$env:USERNAME:(R)"
   ```

### 2.3 Создание SSH конфигурации

1. **Создайте файл конфигурации SSH:**
   ```powershell
   # Создайте директорию .ssh (если не существует)
   if (-not (Test-Path ~/.ssh)) {
       New-Item -ItemType Directory -Path ~/.ssh
   }
   
   # Создайте файл config
   New-Item -ItemType File -Path ~/.ssh/config -Force
   ```

2. **Откройте файл в редакторе:**
   ```powershell
   notepad ~/.ssh/config
   ```
   
   Или откройте в Cursor:
   ```powershell
   cursor ~/.ssh/config
   ```

3. **Добавьте конфигурацию сервера:**
   ```
   Host rocketcloud-vps
       HostName 45.156.21.149
       User root
       IdentityFile ~/.ssh/id_ed25519
       ServerAliveInterval 30
       ServerAliveCountMax 10
       TCPKeepAlive yes
       Compression yes
       ConnectTimeout 120
   ```
   
   **Замените:**
   - `45.156.21.149` - на IP адрес вашего сервера
   - `root` - на имя пользователя на сервере (если отличается)
   - `~/.ssh/id_ed25519` - на путь к вашему приватному ключу (если используете другой ключ)

4. **Установите правильные права доступа для config:**
   ```powershell
   # Откройте PowerShell от имени администратора
   icacls "$env:USERPROFILE\.ssh\config" /inheritance:r
   icacls "$env:USERPROFILE\.ssh\config" /grant:r "$env:USERNAME:(R,W)"
   ```

### 2.4 Проверка SSH подключения

```powershell
ssh rocketcloud-vps "echo 'SSH connection successful!'"
```

Если подключение успешно, вы увидите сообщение "SSH connection successful!".

**Если возникают проблемы:**
- Проверьте, что публичный ключ добавлен на сервер
- Проверьте, что SSH сервис запущен на сервере
- Проверьте файрвол на сервере (порт 22 должен быть открыт)
- См. раздел [Устранение проблем](#10-устранение-проблем)

---

## 3. Настройка Remote SSH в Cursor

### 3.1 Установка расширения Remote SSH

1. **Откройте Cursor**
2. **Нажмите на иконку расширений:**
   - Или нажмите `Ctrl+Shift+X`
   - Или выберите View → Extensions

3. **Найдите и установите расширение:**
   - В поиске введите: `Remote - SSH`
   - Выберите расширение "Remote - SSH" от Microsoft
   - Нажмите "Install"

### 3.2 Настройка таймаутов для Remote SSH

1. **Откройте настройки Cursor:**
   - Нажмите `Ctrl+,` (запятая)
   - Или File → Preferences → Settings

2. **Найдите настройки Remote SSH:**
   - В поиске введите: `remote.SSH.connectTimeout`
   - Установите значение: `120`
   - В поиске введите: `remote.SSH.serverInstallTimeout`
   - Установите значение: `300`

**Или через JSON файл настроек:**
1. Нажмите `Ctrl+Shift+P`
2. Введите: `Preferences: Open User Settings (JSON)`
3. Добавьте следующие настройки:
   ```json
   {
       "remote.SSH.connectTimeout": 120,
       "remote.SSH.serverInstallTimeout": 300,
       "remote.SSH.remoteServerListenOnSocket": true,
       "remote.SSH.useLocalServer": false
   }
   ```

### 3.3 Подключение к серверу

1. **Нажмите `Ctrl+Shift+P`** (или `F1`)
2. **Введите:** `Remote-SSH: Connect to Host`
3. **Выберите:** `rocketcloud-vps` (или имя, которое вы указали в SSH config)

**Или используйте иконку:**
1. Нажмите на зеленую иконку в левом нижнем углу Cursor
2. Выберите "Connect to Host..."
3. Выберите `rocketcloud-vps`

4. **Откроется новое окно Cursor**, подключенное к серверу
5. **При первом подключении:**
   - Введите пароль (если требуется)
   - Подтвердите установку Cursor Server на удаленном сервере
   - Дождитесь завершения установки (может занять 1-2 минуты)

### 3.4 Открытие папки проекта на сервере

1. **После подключения к серверу:**
   - Нажмите `Ctrl+K Ctrl+O` (или File → Open Folder)
   - Или нажмите на кнопку "Open Folder" в окне приветствия

2. **Введите путь к проекту:**
   ```
   /opt/waifu-bot
   ```
   (или другой путь, где находится проект на сервере)

3. **Нажмите "OK"**

**Или через терминал:**
1. Откройте терминал в Cursor: `Ctrl+`` ` (обратная кавычка)
2. Выполните:
   ```bash
   cd /opt/waifu-bot
   code .
   ```

---

## 4. Установка необходимых расширений

После подключения к серверу нужно установить расширения **на удаленном сервере**:

### 4.1 Обязательные расширения

1. **Python** (Microsoft)
   - Автодополнение, отладка, линтинг для Python
   - Поиск: `Python`
   - Установите расширение от Microsoft

2. **Pylance** (Microsoft)
   - Продвинутое автодополнение и анализ кода для Python
   - Обычно устанавливается автоматически с Python
   - Если нет, найдите и установите: `Pylance`

3. **SQLTools** (mtxr)
   - Для работы с базами данных
   - Поиск: `SQLTools`
   - Установите расширение от mtxr

### 4.2 Рекомендуемые расширения

1. **GitLens** (GitKraken)
   - Улучшенная работа с Git
   - Поиск: `GitLens`

2. **Error Lens** (Alexander)
   - Показывает ошибки прямо в коде
   - Поиск: `Error Lens`

3. **Better Comments** (Aaron Bond)
   - Улучшенное отображение комментариев
   - Поиск: `Better Comments`

4. **Prettier** (Prettier)
   - Форматирование кода
   - Поиск: `Prettier`

5. **Markdown All in One** (Yu Zhang)
   - Работа с Markdown файлами
   - Поиск: `Markdown All in One`

### 4.3 Важно

**Расширения устанавливаются отдельно:**
- На локальной машине (для работы с локальными файлами)
- На удаленном сервере (для работы с файлами на сервере)

После подключения к серверу, расширения, установленные локально, не будут работать автоматически. Нужно установить их снова на удаленном сервере.

---

## 5. Настройка Python окружения

### 5.1 Выбор Python интерпретатора

1. **После подключения к серверу и открытия проекта:**
   - Нажмите `Ctrl+Shift+P`
   - Введите: `Python: Select Interpreter`

2. **Выберите интерпретатор:**
   - Если используется виртуальное окружение: `/opt/waifu-bot/venv/bin/python`
   - Если используется системный Python: `/usr/bin/python3`
   - Или укажите другой путь к Python

### 5.2 Проверка версии Python

В терминале Cursor выполните:

```bash
python3 --version
```

Должно быть: `Python 3.11.x` или выше

### 5.3 Установка зависимостей проекта

1. **Откройте терминал в Cursor:** `Ctrl+`` ` (обратная кавычка)

2. **Перейдите в директорию проекта:**
   ```bash
   cd /opt/waifu-bot
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```
   
   Или если используется виртуальное окружение:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

### 5.4 Настройка линтера и форматтера

1. **Откройте настройки:** `Ctrl+,`
2. **Найдите:** `python.linting.enabled`
3. **Убедитесь, что включено:** `true`
4. **Найдите:** `python.linting.pylintEnabled`
5. **При необходимости установите:** `false` (если не используете pylint)
6. **Найдите:** `python.formatting.provider`
7. **Выберите:** `autopep8` или `black`

---

## 6. Настройка проекта

### 6.1 Клонирование проекта (если проект еще не на сервере)

Если проект нужно клонировать на сервер:

```bash
# На сервере
cd /opt
git clone https://github.com/ваш-username/waifu-bot.git
cd waifu-bot
```

### 6.2 Настройка переменных окружения

1. **Создайте файл `.env` в корне проекта:**
   ```bash
   cd /opt/waifu-bot
   nano .env
   ```

2. **Добавьте необходимые переменные:**
   ```env
   BOT_TOKEN=ваш_токен_бота
   DATABASE_URL=postgresql+asyncpg://user:password@host:port/database
   REDIS_URL=redis://host:port/0
   ENV=production
   ```

3. **Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 6.3 Настройка структуры проекта

Убедитесь, что структура проекта правильная:

```
/opt/waifu-bot/
├── src/
│   └── bot/
│       ├── __init__.py
│       ├── main.py
│       ├── config.py
│       └── ...
├── webapp/
│   ├── app.js
│   └── ...
├── requirements.txt
├── .env
├── README.md
└── ...
```

### 6.4 Создание файла настроек Cursor для проекта

Создайте файл `.vscode/settings.json` в корне проекта:

```json
{
    "python.defaultInterpreterPath": "/opt/waifu-bot/venv/bin/python",
    "python.linting.enabled": true,
    "python.linting.pylintEnabled": false,
    "python.formatting.provider": "autopep8",
    "files.exclude": {
        "**/__pycache__": true,
        "**/*.pyc": true
    },
    "editor.formatOnSave": true,
    "editor.codeActionsOnSave": {
        "source.organizeImports": true
    }
}
```

---

## 7. Настройка Git

### 7.1 Настройка Git на сервере

1. **Проверьте, установлен ли Git:**
   ```bash
   git --version
   ```

2. **Если не установлен, установите:**
   ```bash
   apt update && apt install -y git
   ```

### 7.2 Настройка Git конфигурации

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### 7.3 Настройка SSH ключа для Git (если используется GitHub)

1. **Проверьте, добавлен ли SSH ключ на GitHub:**
   - Перейдите на https://github.com/settings/keys
   - Проверьте, есть ли ваш публичный ключ

2. **Если нет, добавьте:**
   - Скопируйте публичный ключ: `cat ~/.ssh/id_ed25519.pub`
   - На GitHub: Settings → SSH and GPG keys → New SSH key
   - Вставьте ключ и сохраните

### 7.4 Проверка подключения к репозиторию

```bash
cd /opt/waifu-bot
git remote -v
git status
```

---

## 8. Проверка работоспособности

### 8.1 Проверка подключения к серверу

- ✅ Подключение к серверу через SSH работает
- ✅ Cursor успешно подключен к серверу
- ✅ Проект открыт в Cursor

### 8.2 Проверка Python окружения

В терминале Cursor:

```bash
python3 --version
pip list | grep -E "aiogram|fastapi|sqlalchemy"
```

### 8.3 Проверка подключения к базе данных

```bash
python3 -c "
from bot.config import get_settings
settings = get_settings()
print(f'Database URL: {settings.database_url[:30]}...')
"
```

### 8.4 Проверка синтаксиса Python файлов

```bash
python3 -m py_compile src/bot/main.py
```

Если ошибок нет, значит синтаксис правильный.

### 8.5 Запуск бота (тестовый)

```bash
cd /opt/waifu-bot
python3 -m bot.main
```

**Не оставляйте бота запущенным в терминале Cursor для продакшена!** Используйте systemd или другой менеджер процессов.

---

## 9. Полезные команды и настройки

### 9.1 Горячие клавиши Cursor

- `Ctrl+Shift+P` - Палитра команд
- `Ctrl+`` ` - Открыть/закрыть терминал
- `Ctrl+K Ctrl+O` - Открыть папку
- `Ctrl+B` - Показать/скрыть боковую панель
- `Ctrl+Shift+E` - Обозреватель файлов
- `Ctrl+Shift+G` - Git
- `Ctrl+Shift+D` - Отладка
- `Ctrl+,` - Настройки
- `Ctrl+K Ctrl+S` - Горячие клавиши

### 9.2 Полезные команды в терминале

```bash
# Перезапуск SSH соединения
Ctrl+Shift+P → "Remote-SSH: Reload Window"

# Переподключение к серверу
Ctrl+Shift+P → "Remote-SSH: Connect to Host"

# Открытие терминала на сервере
Ctrl+`` `

# Проверка статуса Git
git status

# Просмотр логов
git log --oneline -10

# Просмотр изменений
git diff

# Коммит изменений
git add .
git commit -m "Описание изменений"
git push
```

### 9.3 Настройка терминала

В настройках Cursor (`Ctrl+,`):

- `terminal.integrated.defaultProfile.windows` - Профиль терминала по умолчанию
- `terminal.integrated.fontSize` - Размер шрифта
- `terminal.integrated.cursorBlinking` - Мигание курсора

### 9.4 Настройка автодополнения

В настройках Cursor:

- `editor.suggestSelection` - Выбор первого предложения
- `editor.acceptSuggestionOnCommitCharacter` - Принятие предложения при вводе
- `editor.tabCompletion` - Автодополнение по Tab

---

## 10. Устранение проблем

### 10.1 Проблемы с SSH подключением

**Проблема:** "Connection reset" или "Connection refused"

**Решение:**
1. Проверьте, что SSH сервис запущен на сервере:
   ```bash
   # Через веб-консоль RocketCloud
   systemctl status ssh
   ```

2. Проверьте файрвол:
   ```bash
   ufw status
   # Если нужно открыть порт 22:
   ufw allow 22/tcp
   ```

3. Очистите known_hosts:
   ```powershell
   ssh-keygen -R 45.156.21.149
   ```

4. Проверьте SSH конфигурацию:
   ```powershell
   cat ~/.ssh/config
   ```

### 10.2 Проблемы с установкой Cursor Server

**Проблема:** "Failed to install server within the timeout"

**Решение:**
1. Увеличьте таймауты в настройках Cursor (см. раздел 3.2)
2. Проверьте свободную память на сервере:
   ```bash
   free -h
   ```
3. Создайте swap файл (если памяти мало):
   ```bash
   fallocate -l 2G /swapfile
   chmod 600 /swapfile
   mkswap /swapfile
   swapon /swapfile
   echo "/swapfile none swap sw 0 0" >> /etc/fstab
   ```

### 10.3 Проблемы с Python интерпретатором

**Проблема:** Python не найден или неправильная версия

**Решение:**
1. Проверьте версию Python:
   ```bash
   python3 --version
   ```

2. Установите Python 3.11 (если нужно):
   ```bash
   apt update
   apt install -y python3.11 python3.11-venv python3.11-dev
   ```

3. Выберите правильный интерпретатор в Cursor:
   - `Ctrl+Shift+P` → "Python: Select Interpreter"

### 10.4 Проблемы с зависимостями

**Проблема:** Ошибки при установке зависимостей

**Решение:**
1. Обновите pip:
   ```bash
   pip install --upgrade pip
   ```

2. Установите системные зависимости:
   ```bash
   apt update
   apt install -y build-essential python3-dev libpq-dev
   ```

3. Установите зависимости проекта:
   ```bash
   pip install -r requirements.txt
   ```

### 10.5 Проблемы с правами доступа

**Проблема:** "Permission denied" при работе с файлами

**Решение:**
1. Проверьте права доступа:
   ```bash
   ls -la /opt/waifu-bot
   ```

2. Установите правильные права:
   ```bash
   chown -R $USER:$USER /opt/waifu-bot
   chmod -R 755 /opt/waifu-bot
   ```

### 10.6 Проблемы с расширениями

**Проблема:** Расширения не работают на удаленном сервере

**Решение:**
1. Убедитесь, что расширения установлены на удаленном сервере:
   - После подключения к серверу, откройте Extensions
   - Проверьте, что расширения установлены (должна быть метка "Installed")

2. Перезагрузите окно Cursor:
   - `Ctrl+Shift+P` → "Developer: Reload Window"

### 10.7 Проблемы с Git

**Проблема:** Git не работает или не подключается к репозиторию

**Решение:**
1. Проверьте конфигурацию Git:
   ```bash
   git config --list
   ```

2. Проверьте SSH ключ для GitHub:
   ```bash
   ssh -T git@github.com
   ```

3. Если нужно, добавьте SSH ключ на GitHub (см. раздел 7.3)

---

## 📝 Чеклист настройки

Используйте этот чеклист для проверки настройки:

- [ ] Cursor установлен и запускается
- [ ] SSH ключ создан и скопирован на сервер
- [ ] SSH конфигурация создана и работает
- [ ] Подключение к серверу через SSH работает
- [ ] Remote SSH расширение установлено
- [ ] Настройки таймаутов для Remote SSH применены
- [ ] Успешное подключение к серверу через Cursor
- [ ] Проект открыт на сервере
- [ ] Python расширения установлены (локально и на сервере)
- [ ] Python интерпретатор выбран правильно
- [ ] Зависимости проекта установлены
- [ ] Переменные окружения настроены (.env файл)
- [ ] Git настроен и работает
- [ ] Все проверки работоспособности пройдены

---

## 🎉 Готово!

После выполнения всех шагов, вы должны иметь полностью настроенный Cursor для работы с проектом Waifu Bot на удаленном сервере.

**Следующие шаги:**
- Начните разработку!
- Используйте Git для контроля версий
- Используйте отладчик Cursor для отладки кода
- Настройте автоматический деплой (если нужно)

---

## 📞 Дополнительная помощь

Если возникли проблемы, проверьте:
- [Документация Cursor](https://cursor.sh/docs)
- [Документация Remote SSH](https://code.visualstudio.com/docs/remote/ssh)
- [Документация Python в VS Code/Cursor](https://code.visualstudio.com/docs/languages/python)

---

**Последнее обновление:** 2024
**Версия инструкции:** 1.0

