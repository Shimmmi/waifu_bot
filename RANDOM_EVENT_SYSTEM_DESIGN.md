# 🎯 Расширенная система случайных событий

## 📋 Обзор

Документ описывает детальный план разработки расширенной системы случайных событий для Waifu Bot, включающей:
1. Множественные базовые события (проверка общей мощи)
2. События для конкретных рас, профессий и национальностей с автоматической фильтрацией
3. События для конкретных редкостей
4. События для различных основных характеристик

---

## 🎲 Типы событий

### 1. Базовые события (Overall Power)
События, проверяющие общую мощь вайфу (сумма всех характеристик + уровень + динамические бонусы).

| ID | Название | Описание | Сортировка |
|----|----------|----------|------------|
| `power_arena` | Арена силы | Проверка общей мощи вайфу | По общей мощи (power) |
| `total_combat` | Общий бой | Сражение с использованием всех навыков | По общей мощи (power) |
| `grand_tournament` | Большой турнир | Турнир, требующий всесторонней подготовки | По общей мощи (power) |
| `ultimate_challenge` | Финальный вызов | Испытание для самых сильных вайфу | По общей мощи (power) |

**Параметры фильтрации:** Нет (доступны все вайфу)

---

### 2. События по расам (Race-specific)
События, доступные только для вайфу определенной расы.

| ID | Название | Раса | Описание | Сортировка |
|----|----------|------|----------|------------|
| `elf_magic_contest` | Магический конкурс эльфов | Elf | Демонстрация магических способностей эльфов | По общей мощи (power) |
| `demon_dark_tournament` | Темный турнир демонов | Demon | Соревнование силы и темной магии | По общей мощи (power) |
| `angel_divine_ceremony` | Божественная церемония | Angel | Проверка чистоты и силы ангелов | По общей мощи (power) |
| `vampire_night_ball` | Ночной бал вампиров | Vampire | Элегантное соревнование в ночи | По общей мощи (power) |
| `dragon_flight_race` | Гонка драконов | Dragon | Испытание скорости и силы драконов | По общей мощи (power) |
| `beast_wild_hunt` | Дикая охота | Beast | Проверка инстинктов и силы зверей | По общей мощи (power) |
| `fairy_enchantment_show` | Шоу очарования фей | Fairy | Демонстрация магии и красоты фей | По общей мощи (power) |
| `human_worldly_skills` | Мирские навыки | Human | Проверка разнообразия навыков людей | По общей мощи (power) |

**Параметры фильтрации:** `race` = указанная раса

---

### 3. События по профессиям (Profession-specific)
События, доступные только для вайфу определенной профессии.

| ID | Название | Профессия | Описание | Сортировка |
|----|----------|-----------|----------|------------|
| `warrior_melee_tournament` | Турнир ближнего боя | Warrior | Сражение на мечах и щитах | По общей мощи (power) |
| `mage_spell_casting` | Турнир заклинателей | Mage | Соревнование в магических способностях | По общей мощи (power) |
| `assassin_shadow_contest` | Конкурс теней | Assassin | Испытание скрытности и ловкости | По общей мощи (power) |
| `knight_honor_duel` | Рыцарский поединок | Knight | Поединок чести и доблести | По общей мощи (power) |
| `archer_precision_contest` | Конкурс точности | Archer | Проверка меткости и скорости | По общей мощи (power) |
| `healer_healing_competition` | Соревнование целителей | Healer | Проверка навыков исцеления | По общей мощи (power) |
| `merchant_trading_challenge` | Торговое испытание | Merchant | Проверка навыков торговли | По общей мощи (power) |

**Параметры фильтрации:** `profession` = указанная профессия

---

### 4. События по национальностям (Nationality-specific)
События, доступные только для вайфу определенной национальности.

| ID | Название | Национальность | Описание | Сортировка |
|----|----------|----------------|----------|------------|
| `japanese_tradition_festival` | Фестиваль традиций | JP | Демонстрация японской культуры | По общей мощи (power) |
| `chinese_martial_arts` | Турнир боевых искусств | CN | Проверка навыков боевых искусств | По общей мощи (power) |
| `korean_kpop_showcase` | K-pop шоукейс | KR | Соревнование в танцах и пении | По общей мощи (power) |
| `american_superhero_contest` | Конкурс супергероев | US | Проверка силы и героизма | По общей мощи (power) |
| `british_royal_gala` | Королевский гала | GB | Элегантное соревнование | По общей мощи (power) |
| `french_art_salon` | Салон искусств | FR | Демонстрация художественных талантов | По общей мощи (power) |
| `german_engineering_show` | Выставка инженерии | DE | Проверка интеллекта и точности | По общей мощи (power) |
| `italian_culinary_master` | Кулинарный мастер | IT | Соревнование в кулинарии | По общей мощи (power) |
| `russian_winter_games` | Зимние игры | RU | Испытание силы и выносливости | По общей мощи (power) |
| `brazilian_carnival_dance` | Карнавальный танец | BR | Проверка танцевальных навыков | По общей мощи (power) |
| `indian_spiritual_quest` | Духовный квест | IN | Проверка мудрости и духовности | По общей мощи (power) |
| `canadian_wilderness_race` | Гонка в дикой природе | CA | Испытание выживания и силы | По общей мощи (power) |

**Параметры фильтрации:** `nationality` = указанная национальность

---

### 5. События по редкости (Rarity-specific)
События, доступные только для вайфу определенной редкости или выше.

| ID | Название | Минимальная редкость | Описание | Сортировка |
|----|----------|---------------------|----------|------------|
| `common_rookie_arena` | Арена новичков | Common | Событие для начинающих вайфу | По общей мощи (power) |
| `uncommon_rising_stars` | Восходящие звезды | Uncommon | Турнир для развивающихся вайфу | По общей мощи (power) |
| `rare_elite_tournament` | Элитный турнир | Rare | Соревнование для редких вайфу | По общей мощи (power) |
| `epic_legendary_battle` | Легендарная битва | Epic | Испытание для эпических вайфу | По общей мощи (power) |
| `legendary_ultimate_showdown` | Финальная схватка | Legendary | Вызов только для легендарных вайфу | По общей мощи (power) |

**Параметры фильтрации:** `rarity` >= указанная редкость

---

### 6. События по основным характеристикам (Primary Stat Challenges)
События, сортирующие вайфу по конкретной основной характеристике.

#### 6.1. Сила (Power)
| ID | Название | Основная характеристика | Описание | Сортировка |
|----|----------|------------------------|----------|------------|
| `strength_arena` | Арена силы | power | Проверка физической силы | По power (убывание) |
| `power_lifting_contest` | Соревнование по тяжелой атлетике | power | Поднятие тяжестей | По power (убывание) |
| `might_tournament` | Турнир могущества | power | Испытание силы | По power (убывание) |
| `brute_force_challenge` | Вызов грубой силы | power | Проверка физической мощи | По power (убывание) |
| `destruction_competition` | Соревнование разрушителей | power | Испытание разрушающей силы | По power (убывание) |

**Параметры фильтрации:** Нет (доступны все вайфу)
**Сортировка:** По `stats.power` (убывание)

#### 6.2. Очарование (Charm)
| ID | Название | Основная характеристика | Описание | Сортировка |
|----|----------|------------------------|----------|------------|
| `charm_contest` | Конкурс очарования | charm | Проверка привлекательности | По charm (убывание) |
| `beauty_pageant` | Конкурс красоты | charm | Соревнование красоты | По charm (убывание) |
| `charisma_show` | Шоу харизмы | charm | Демонстрация обаяния | По charm (убывание) |
| `attraction_battle` | Битва притягательности | charm | Проверка магнетизма | По charm (убывание) |
| `elegance_competition` | Соревнование элегантности | charm | Испытание изящества | По charm (убывание) |

**Параметры фильтрации:** Нет (доступны все вайфу)
**Сортировка:** По `stats.charm` (убывание)

#### 6.3. Интеллект (Intellect)
| ID | Название | Основная характеристика | Описание | Сортировка |
|----|----------|------------------------|----------|------------|
| `intellect_quiz` | Интеллектуальная викторина | intellect | Проверка знаний | По intellect (убывание) |
| `mind_puzzle_challenge` | Головоломка разума | intellect | Решение сложных задач | По intellect (убывание) |
| `strategy_tournament` | Турнир стратегов | intellect | Проверка стратегического мышления | По intellect (убывание) |
| `wisdom_contest` | Конкурс мудрости | intellect | Испытание мудрости | По intellect (убывание) |
| `brain_power_show` | Шоу интеллекта | intellect | Демонстрация умственных способностей | По intellect (убывание) |

**Параметры фильтрации:** Нет (доступны все вайфу)
**Сортировка:** По `stats.intellect` (убывание)

#### 6.4. Скорость (Speed)
| ID | Название | Основная характеристика | Описание | Сортировка |
|----|----------|------------------------|----------|------------|
| `speed_race` | Гонка скорости | speed | Проверка быстроты | По speed (убывание) |
| `agility_contest` | Конкурс ловкости | speed | Испытание проворства | По speed (убывание) |
| `flash_challenge` | Вызов молнии | speed | Проверка скорости реакции | По speed (убывание) |
| `rapid_tournament` | Турнир скорости | speed | Соревнование в быстроте | По speed (убывание) |
| `lightning_strike_competition` | Соревнование молний | speed | Испытание молниеносной скорости | По speed (убывание) |

**Параметры фильтрации:** Нет (доступны все вайфу)
**Сортировка:** По `stats.speed` (убывание)

#### 6.5. Удача (Luck)
| ID | Название | Основная характеристика | Описание | Сортировка |
|----|----------|------------------------|----------|------------|
| `luck_roulette` | Рулетка удачи | luck | Проверка везения | По luck (убывание) |
| `fortune_wheel` | Колесо фортуны | luck | Испытание удачи | По luck (убывание) |
| `chance_competition` | Соревнование шансов | luck | Проверка случайности | По luck (убывание) |
| `lucky_draw` | Счастливая жеребьевка | luck | Испытание фортуны | По luck (убывание) |
| `fate_tournament` | Турнир судьбы | luck | Проверка благосклонности судьбы | По luck (убывание) |

**Параметры фильтрации:** Нет (доступны все вайфу)
**Сортировка:** По `stats.luck` (убывание)

#### 6.6. Привязанность (Affection)
| ID | Название | Основная характеристика | Описание | Сортировка |
|----|----------|------------------------|----------|------------|
| `affection_ceremony` | Церемония привязанности | affection | Проверка близости | По affection (убывание) |
| `bond_challenge` | Вызов связи | affection | Испытание привязанности | По affection (убывание) |
| `love_contest` | Конкурс любви | affection | Соревнование в любви | По affection (убывание) |
| `devotion_tournament` | Турнир преданности | affection | Проверка верности | По affection (убывание) |
| `connection_competition` | Соревнование связи | affection | Испытание эмоциональной связи | По affection (убывание) |

**Параметры фильтрации:** Нет (доступны все вайфу)
**Сортировка:** По `stats.affection` (убывание)

---

## 🗄️ Структура данных события

### Формат события в `EVENTS` (data_tables.py)

```python
EVENTS = {
    "event_id": {
        "name": "Название события",
        "description": "Описание события",
        "base_stats": ["stat1", "stat2", "stat3"],  # Для расчета очков
        "profession_bonus": "ProfessionName",  # Опционально
        
        # Новые поля для расширенной системы:
        "filter_type": "none" | "race" | "profession" | "nationality" | "rarity" | "primary_stat",
        "filter_value": "Human" | "Warrior" | "JP" | "Rare" | "power",  # Значение фильтра
        "sort_by": "power" | "power_stat" | "charm_stat" | "intellect_stat" | "speed_stat" | "luck_stat" | "affection_stat",
        # "power" = общая мощь вайфу
        # "power_stat" = только stats.power
        # и т.д. для других характеристик
        
        "rarity_min": None | "Common" | "Uncommon" | "Rare" | "Epic" | "Legendary",  # Для rarity событий
    }
}
```

### Примеры конфигураций

```python
# Базовое событие (общая мощь)
{
    "power_arena": {
        "name": "Арена силы",
        "description": "Проверка общей мощи вайфу",
        "base_stats": ["power", "charm", "intellect", "speed", "luck", "affection"],
        "profession_bonus": None,
        "filter_type": "none",
        "sort_by": "power"
    }
}

# Событие по расе
{
    "elf_magic_contest": {
        "name": "Магический конкурс эльфов",
        "description": "Демонстрация магических способностей эльфов",
        "base_stats": ["intellect", "charm", "luck"],
        "profession_bonus": "Mage",
        "filter_type": "race",
        "filter_value": "Elf",
        "sort_by": "power"
    }
}

# Событие по профессии
{
    "warrior_melee_tournament": {
        "name": "Турнир ближнего боя",
        "description": "Сражение на мечах и щитах",
        "base_stats": ["power", "speed", "luck"],
        "profession_bonus": "Warrior",
        "filter_type": "profession",
        "filter_value": "Warrior",
        "sort_by": "power"
    }
}

# Событие по национальности
{
    "japanese_tradition_festival": {
        "name": "Фестиваль традиций",
        "description": "Демонстрация японской культуры",
        "base_stats": ["charm", "intellect", "luck"],
        "profession_bonus": None,
        "filter_type": "nationality",
        "filter_value": "JP",
        "sort_by": "power"
    }
}

# Событие по редкости
{
    "rare_elite_tournament": {
        "name": "Элитный турнир",
        "description": "Соревнование для редких вайфу",
        "base_stats": ["power", "charm", "intellect", "speed", "luck", "affection"],
        "profession_bonus": None,
        "filter_type": "rarity",
        "filter_value": "Rare",
        "rarity_min": "Rare",  # Минимальная редкость
        "sort_by": "power"
    }
}

# Событие по характеристике (сила)
{
    "strength_arena": {
        "name": "Арена силы",
        "description": "Проверка физической силы",
        "base_stats": ["power"],
        "profession_bonus": "Warrior",
        "filter_type": "none",
        "sort_by": "power_stat"  # Сортировка по stats.power
    }
}

# Событие по характеристике (интеллект)
{
    "intellect_quiz": {
        "name": "Интеллектуальная викторина",
        "description": "Проверка знаний",
        "base_stats": ["intellect"],
        "profession_bonus": "Scholar",
        "filter_type": "none",
        "sort_by": "intellect_stat"  # Сортировка по stats.intellect
    }
}
```

---

## 🔄 Логика фильтрации и сортировки

### 1. Фильтрация вайфу при выборе

Когда игрок участвует в событии и должен выбрать вайфу:

#### Шаг 1: Применение фильтров

```python
def filter_waifus_for_event(waifus: List[Waifu], event_config: Dict) -> List[Waifu]:
    """
    Фильтрует список вайфу в соответствии с требованиями события
    
    Args:
        waifus: Список всех вайфу игрока
        event_config: Конфигурация события из EVENTS
        
    Returns:
        Отфильтрованный список вайфу
    """
    filtered = waifus.copy()
    
    filter_type = event_config.get("filter_type", "none")
    filter_value = event_config.get("filter_value")
    
    if filter_type == "race":
        filtered = [w for w in filtered if w.race == filter_value]
    elif filter_type == "profession":
        filtered = [w for w in filtered if w.profession == filter_value]
    elif filter_type == "nationality":
        filtered = [w for w in filtered if w.nationality == filter_value]
    elif filter_type == "rarity":
        rarity_order = ["Common", "Uncommon", "Rare", "Epic", "Legendary"]
        min_rarity_index = rarity_order.index(filter_value) if filter_value in rarity_order else 0
        filtered = [
            w for w in filtered 
            if rarity_order.index(w.rarity) >= min_rarity_index
        ]
    # filter_type == "none" или "primary_stat" - без фильтрации
    
    return filtered
```

#### Шаг 2: Сортировка вайфу

```python
def sort_waifus_for_event(waifus: List[Waifu], event_config: Dict) -> List[Waifu]:
    """
    Сортирует вайфу для отображения в порядке выбора
    
    Args:
        waifus: Список отфильтрованных вайфу
        event_config: Конфигурация события
        
    Returns:
        Отсортированный список вайфу (от лучшего к худшему)
    """
    sort_by = event_config.get("sort_by", "power")
    
    def get_sort_key(waifu: Waifu) -> float:
        if sort_by == "power":
            # Общая мощь вайфу
            from bot.services.waifu_power import calculate_waifu_power
            skill_effects = {}  # Получить из пользователя
            return calculate_waifu_power({
                "stats": waifu.stats,
                "dynamic": waifu.dynamic,
                "level": waifu.level
            }, skill_effects)
        
        elif sort_by == "power_stat":
            return waifu.stats.get("power", 0)
        elif sort_by == "charm_stat":
            return waifu.stats.get("charm", 0)
        elif sort_by == "intellect_stat":
            return waifu.stats.get("intellect", 0)
        elif sort_by == "speed_stat":
            return waifu.stats.get("speed", 0)
        elif sort_by == "luck_stat":
            return waifu.stats.get("luck", 0)
        elif sort_by == "affection_stat":
            return waifu.stats.get("affection", 0)
        else:
            # По умолчанию по общей мощи
            return calculate_waifu_power({...}, {})
    
    sorted_waifus = sorted(waifus, key=get_sort_key, reverse=True)
    return sorted_waifus
```

### 2. Выбор вайфу в интерфейсе

Когда игрок открывает выбор вайфу для события:

1. **Загрузить все вайфу игрока**
2. **Применить фильтры** (race/profession/nationality/rarity)
3. **Отсортировать по указанной характеристике** (power/power_stat/charm_stat/etc.)
4. **Отобразить отсортированный список** (от лучшего к худшему)
5. **Подсветить вайфу с максимальным значением** характеристики для события

---

## 📊 Расчет очков в событии

### Формула расчета очков

```python
def calculate_event_score(waifu: Dict, event_type: str) -> Tuple[float, str]:
    """
    Вычисляет очки вайфу в событии с учетом всех факторов
    """
    event = EVENTS[event_type]
    stats = waifu.get("stats", {})
    dynamic = waifu.get("dynamic", {})
    
    # Базовые очки из характеристик
    base_score = 0
    for stat in event["base_stats"]:
        if stat in stats:
            multiplier = random.uniform(0.8, 1.2)  # Случайность
            base_score += stats[stat] * multiplier
    
    # Бонус за профессию
    profession_bonus = 1.0
    if event.get("profession_bonus") and waifu.get("profession") == event["profession_bonus"]:
        profession_bonus = 1.25
    
    # Бонусы от динамических характеристик
    mood_bonus = dynamic.get("mood", 50) / 100
    loyalty_bonus = dynamic.get("loyalty", 50) / 100
    
    # Бонус за соответствие фильтру (для race/profession/nationality событий)
    filter_bonus = 1.0
    filter_type = event.get("filter_type")
    if filter_type == "race" and waifu.get("race") == event.get("filter_value"):
        filter_bonus = 1.15  # +15% за правильную расу
    elif filter_type == "profession" and waifu.get("profession") == event.get("filter_value"):
        filter_bonus = 1.15  # +15% за правильную профессию
    elif filter_type == "nationality" and waifu.get("nationality") == event.get("filter_value"):
        filter_bonus = 1.15  # +15% за правильную национальность
    elif filter_type == "rarity":
        # Бонус за редкость: выше редкость = выше бонус
        rarity_multipliers = {
            "Common": 1.0,
            "Uncommon": 1.1,
            "Rare": 1.2,
            "Epic": 1.3,
            "Legendary": 1.5
        }
        filter_bonus = rarity_multipliers.get(waifu.get("rarity"), 1.0)
    
    # Бонус за уровень
    level_bonus = 1.0 + (waifu.get("level", 1) - 1) * 0.02  # +2% за каждый уровень выше 1
    
    # Итоговый счет
    final_score = (
        base_score 
        * profession_bonus 
        * filter_bonus
        * level_bonus
        * (0.8 + mood_bonus * 0.2) 
        * (0.9 + loyalty_bonus * 0.1)
    )
    
    # Добавляем случайность для интереса
    final_score *= random.uniform(0.9, 1.1)
    
    return round(final_score, 2), event["name"]
```

---

## 🎨 UI/UX изменения

### 1. Выбор вайфу для события

**Текущий интерфейс:**
- Список всех вайфу
- Сортировка не учитывает событие

**Новый интерфейс:**
- При открытии выбора вайфу:
  - Показать информацию о событии (название, требования)
  - Применить фильтры (показать только подходящие вайфу)
  - Отсортировать по релевантной характеристике
  - Подсветить вайфу с максимальным значением
  - Показать подсказку: "Рекомендуется: [Имя вайфу] (лучшая по [характеристике])"

### 2. Отображение списка вайфу

**Для событий по характеристике (primary_stat):**
- Показать значение характеристики крупно
- Отсортировать от большего к меньшему
- Подсветить вайфу с максимальным значением

**Для событий с фильтрами (race/profession/nationality/rarity):**
- Показать только подходящие вайфу
- Отсортировать по общей мощи (после фильтрации)
- Показать подсказку: "Только [race/profession/nationality/rarity]"

### 3. Информация о событии

Добавить в сообщение о событии:
```
🎯 Название события: [Название]
📝 Описание: [Описание]

🔍 Требования:
   • [Фильтр, если есть] (например, "Раса: Elf")
   • Лучшая характеристика: [stat_name]

💡 Рекомендация: Выберите вайфу с высоким [stat_name]
```

---

## 📝 Итоговая таблица всех событий

### Сводная таблица

| Категория | Количество событий | ID префикс | Примеры |
|-----------|-------------------|------------|---------|
| Overall Power | 4 | `power_`, `total_`, `grand_`, `ultimate_` | `power_arena`, `total_combat` |
| Race-specific | 8 | `{race}_` | `elf_magic_contest`, `demon_dark_tournament` |
| Profession-specific | 7 | `{profession}_` | `warrior_melee_tournament`, `mage_spell_casting` |
| Nationality-specific | 12 | `{nationality}_` | `japanese_tradition_festival`, `chinese_martial_arts` |
| Rarity-specific | 5 | `{rarity}_` | `common_rookie_arena`, `rare_elite_tournament` |
| Primary Stat Challenges | 30 | `{stat}_` | `strength_arena`, `intellect_quiz`, `charm_contest` |
| **ИТОГО** | **66 событий** | - | - |

---

## 🚀 План реализации

### Фаза 1: Базовая структура
1. Расширить структуру `EVENTS` в `data_tables.py`
2. Добавить поля `filter_type`, `filter_value`, `sort_by`, `rarity_min`
3. Обновить функцию `calculate_event_score` для учета фильтров

### Фаза 2: Фильтрация
1. Реализовать `filter_waifus_for_event()`
2. Реализовать `sort_waifus_for_event()`
3. Обновить UI выбора вайфу для применения фильтров

### Фаза 3: Сортировка
1. Обновить интерфейс выбора вайфу
2. Реализовать сортировку по характеристикам
3. Добавить подсветку рекомендованной вайфу

### Фаза 4: Контент
1. Добавить все 66 событий в `EVENTS`
2. Протестировать каждое событие
3. Балансировка наград

### Фаза 5: UI/UX улучшения
1. Добавить информационные сообщения о событиях
2. Улучшить визуальное отображение фильтров
3. Добавить подсказки и рекомендации

---

## ✅ Контрольный список

- [ ] Расширить структуру `EVENTS` в `data_tables.py`
- [ ] Реализовать `filter_waifus_for_event()`
- [ ] Реализовать `sort_waifus_for_event()`
- [ ] Обновить `calculate_event_score()` для учета фильтров
- [ ] Обновить UI выбора вайфу для применения фильтров
- [ ] Добавить все 66 событий в `EVENTS`
- [ ] Протестировать фильтрацию и сортировку
- [ ] Добавить информационные сообщения о событиях
- [ ] Балансировка наград для разных типов событий
- [ ] Обновить документацию API

---

## 📌 Примечания

1. **Обратная совместимость**: Старые события (dance, hunt, quiz, cooking, singing) должны продолжать работать без изменений. Добавить им значения по умолчанию для новых полей.

2. **Производительность**: Фильтрация и сортировка должны выполняться быстро даже при большом количестве вайфу у игрока (до 1000 вайфу).

3. **Балансировка**: Награды должны быть справедливыми для всех типов событий. События с фильтрами могут давать немного больше наград как компенсацию за ограниченный выбор.

4. **Рандомизация**: Случайность в расчете очков (random.uniform) должна оставаться для предотвращения предсказуемости результатов.

---

**Документ подготовлен:** [Дата]
**Версия:** 1.0
**Статус:** Готов к реализации
